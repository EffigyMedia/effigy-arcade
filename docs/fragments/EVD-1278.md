+++
id                = "EVD-1278"
kind              = "evidence"
status            = "recorded"
step              = "race-test after the livery review"
unit              = "UNT-419"
command           = "P:\\Code_Continuum\\Projects\\In-Dev\\Effigy_Arcade\\.venv\\Scripts\\python.exe tools/race-test.py"
exit_code         = 1
duration_ms       = 59898
output_bytes      = 984
output_sha        = "a1540cb510840345520df337291effd5"
store_stamp       = "913d730fc55cf1ff"
created           = "2026-09-22T11:29:18-04:00"
created_precision = "second"
supersedes        = []
blocks            = []
caused_by         = []
see_also          = []
+++

### EVD-1278 — step `race-test after the livery review` of [[UNT-419]]

- Ran: `P:\Code_Continuum\Projects\In-Dev\Effigy_Arcade\.venv\Scripts\python.exe tools/race-test.py`
- Result: **EXIT 1 — the step FAILED**, in 59898 ms
- Output: 984 bytes, sha256 `a1540cb510840345520df337291effd5`
- Store stamp at the time: `913d730fc55cf1ff`

**Output** — head only; the digest above covers the whole of it, so this excerpt cannot be selective without saying so.

```
race-test  .  a race has checkpoints, whatever the toggle says
  browser: installed chrome - no bundled chromium, so the engine version is whatever this machine has - GPU OFF (EFFIGY_NO_GPU), so no frame rate here is comparable
  ok    the garage starts on TEST DRIVE
  ok    and TIMED can be turned off there
  ok    and back on again
  ok    then the mode changes to a race
      TIMED off then on, then a race: 0 board(s)
  FAIL  a race puts checkpoint boards on the road   [none were placed]
  ok    TIMED can be left off
  ok    with the mode set to a race
      TIMED left OFF, then a race: 0 board(s), clock at 119.7
  FAIL  a race with TIMED off still puts boards on the road   [none were placed, though the clock was running]
      the checkpoint flash reads: ''
  FAIL  a checkpoint is actually crossed   [no CHECKPOINT flash appeared in the whole run]
  FAIL  no page errors   [["Uncaught TypeError: Cannot read properties of null (reading 'front')"]]

  4 check(s) failed
```

*Written by `step.py` as a byproduct of running the step. **Nobody attested to this** ([[RLG-081]]).*
