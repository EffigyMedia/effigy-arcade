+++
id                = "RLG-204"
kind              = "ruling"
type              = "defect"
status            = "in-flight"
created           = "2026-09-10T03:22:14-04:00"
created_precision = "second"
source            = "found while building RLG-181, 2026-09-10"
supersedes        = []
blocks            = []
caused_by         = []
see_also          = []
+++

### RLG-204 — copLivery is assigned and never read
- **RULED 2026-09-10T03:22:14-04:00** — TRACKED, NOT STARTED. Found while building [[RLG-181]], not reported by the owner.

**THE RULE IS WRITTEN, THE STATE IS SET, AND NOTHING READS IT.** `copLivery` carries a comment
stating a real design decision: *"A force does not run half its cars in white and half in black on
the same night. The livery is chosen once when the run starts and every cruiser wears it - including
yours, so there is no 'player version' and 'NPC version', just the cars that are out tonight."* The
variable is declared at the top of the file and assigned in exactly one place, when a run resets. It
is read NOWHERE. `COP_PAINT`, the table beside it, is read by one sprite dump and nothing else.

**SO THE FORCE'S COLOUR IS DECIDED AT BOOT AND NOT PER RUN.** The cruiser and the interceptor sprites
are built once with fixed values, which is why the rule reads as if it works: the cars ARE all the
same colour. They are all the same colour in every run, which is not the same claim.

**WHAT [[RLG-181]] CHANGED, AND WHAT IT DELIBERATELY DID NOT.** The assignment named one body -
`optBody === 'CRUISER'` - so a player in the interceptor could never be the car the force matched
itself to. It asks `inForce()` now, which reads the BODY record, the same question the light bar and
the siren ask. That changes no pixel, because nothing reads the result; it stops the dead line from
being wrong for the day somebody wires it up.

**TWO ANSWERS, AND THEY ARE NOT THE SAME SIZE.** Wire it up - the cop sprites would have to be built
per run, or built twice and chosen between, which is real work at boot. Or delete the state and the
comment, and let the fixed colours be the honest description of what the game does. The second is
one line. Do not leave it as it is, because a comment stating a rule the code does not implement is
the kind of thing a later session believes.
- **RULED 2026-09-16T00:57:35-04:00** — OWNER, 2026-09-16, on the unused police livery: "I did not even know ruling 204 was a thing." Left as it is, neither queued nor closed: nobody has asked for it.
- **RULED 2026-09-19T08:19:42-04:00** — SPECIFIED BY THE OWNER, 2026-09-19: the police on the road all wear the SAME paint for a run, drawn from the regular police colours - including the ones the player has not unlocked yet - and NEVER the iridescent paints. "NPC police can use the regular police unlocked paint even before they are unlocked. Only the player doesn't have access to those until they are unlocked." The owner thinks this may already be done; to be verified against the code before any work.
- **RULED 2026-09-19T08:21:13-04:00** — CHECKED 2026-09-19: NOT DONE. copLivery is assigned at the start of each run (WHITE or BLACK) and read by nothing - the source says so - and the police sprites are built once at boot in fixed colours. So the owner's rule (one colour per run for all road police, from the regular police colours including locked ones, never iridescent) is not in the game yet.
- **RULED 2026-09-19T09:23:19-04:00** — BUILT, v0.14.86. Every police car on the road now wears one colour a run, chosen at the start of the run from `NPC_COP_PAINTS` - white, black and the four force colours (force blue, force green, charcoal, silver) - whether or not the player has unlocked them, and never the iridescent paints. A player driving a police car is matched by the force, unless the player's own paint is one the force does not wear (an iridescent one), in which case the force takes a regular colour.

WHAT WAS MISSING: `copLivery` was chosen every run and read by nothing; the police sprites were painted white once, at boot. The four drawings of the force - CRUISER and SUPERCRUISER, rear and front - are now built by `buildCopSprites(livery)` and repainted at each run's start.

PROVEN by `tools/livery-test.py` over 30 runs in a fresh save: all six regular colours appeared, none iridescent, including the four the player has not unlocked; and the drawn CRUISER, read as a fingerprint of its pixels, was different for each of the six liveries. With the repaint removed - the original defect - six liveries drew one car and the check failed. smoke-test passes; duty-test's one failure is its off-shift billing boundary (236 against 240), which failed the same way before this work.

PROCESS NOTE: the unit for this work was opened after the edits were made, not before. The evidence was recorded after it opened.
