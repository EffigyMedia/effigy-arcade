+++
id                = "EVD-1093"
kind              = "evidence"
status            = "recorded"
step              = "skyline-test with the mile of growth"
unit              = "UNT-384"
command           = "P:\\Code_Continuum\\Projects\\In-Dev\\Effigy_Arcade\\.venv\\Scripts\\python.exe tools/skyline-test.py"
exit_code         = 0
duration_ms       = 20870
output_bytes      = 1357
output_sha        = "9e77d2ef778bf6efc9419b2b5eb9aa59"
store_stamp       = "490e2283b956481e"
created           = "2026-09-19T14:29:03-04:00"
created_precision = "second"
supersedes        = []
blocks            = []
caused_by         = []
see_also          = []
+++

### EVD-1093 — step `skyline-test with the mile of growth` of [[UNT-384]]

- Ran: `P:\Code_Continuum\Projects\In-Dev\Effigy_Arcade\.venv\Scripts\python.exe tools/skyline-test.py`
- Result: **exit 0**, in 20870 ms
- Output: 1357 bytes, sha256 `9e77d2ef778bf6efc9419b2b5eb9aa59`
- Store stamp at the time: `490e2283b956481e`

**Output** — head only; the digest above covers the whole of it, so this excerpt cannot be selective without saying so.

```
skyline-test  .  the city is the place's, not the hour's
  browser: installed chrome - no bundled chromium, so the engine version is whatever this machine has
  the place: CITY
  buckets walked: 4 6 7 9 12 17 22 27 32 37

  ok    the run crossed several hour buckets, so the rebuild actually fired
  ok    the silhouette does not change as the day turns
        columns that moved across the whole day: 0 of 1024
  ok    and it still takes the hour's light, so the sprite is not simply frozen
        colour travelled 95 across the day  (rgb(44,28,45) -> rgb(56,35,50))
  ok    and the silhouette covers the same area at every hour

  the windows
  ok    the hour is dark enough for the city to be lit at all
  ok    windows switch on and off as the night goes on
        columns whose lit count changed: 62 of 1024
  ok    and the buildings do not move while their windows do
  ok    and they do not switch in unison
        lit pixels across the night: [419, 408, 431, 425, 449, 437]

  ok    with the window clock held still nothing switches, so the check reads time
        with the clock held at 120s: 0 columns changed

  the same check, with the plan forgotten between samples
  ok    with the plan re-rolled the silhouette check goes red, so it was measuring shape
        columns that moved: 1024 of 1024

  ok    no page errors

all checks passed
```

*Written by `step.py` as a byproduct of running the step. **Nobody attested to this** ([[RLG-081]]).*
