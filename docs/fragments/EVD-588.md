+++
id                = "EVD-588"
kind              = "evidence"
status            = "recorded"
step              = "every one of the seventeen garage bodies draws one silhouette from both ends, and both ends are drawn at one height"
unit              = "UNT-226"
command           = "--store docs/fragments -- .venv/Scripts/python tools/car-ends-test.py"
exit_code         = 127
duration_ms       = 33
output_bytes      = 69
output_sha        = "32d13edb610e49a5ef8a9e774e413581"
store_stamp       = "f9305c96769179bf"
created           = "2026-09-09T15:47:44-04:00"
created_precision = "second"
supersedes        = []
blocks            = []
caused_by         = []
see_also          = []
+++

### EVD-588 — step `every one of the seventeen garage bodies draws one silhouette from both ends, and both ends are drawn at one height` of [[UNT-226]]

- Ran: `--store docs/fragments -- .venv/Scripts/python tools/car-ends-test.py`
- Result: **EXIT 127 — the step FAILED**, in 33 ms
- Output: 69 bytes, sha256 `32d13edb610e49a5ef8a9e774e413581`
- Store stamp at the time: `f9305c96769179bf`

**Output** — head only; the digest above covers the whole of it, so this excerpt cannot be selective without saying so.

```
could not run: [WinError 2] The system cannot find the file specified
```

*Written by `step.py` as a byproduct of running the step. **Nobody attested to this** ([[RLG-081]]).*
