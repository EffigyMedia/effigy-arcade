+++
id                = "RLG-327"
kind              = "ruling"
type              = "defect"
status            = "built"
created           = "2026-09-22T21:06:22-04:00"
created_precision = "second"
source            = "owner 2026-09-22"
supersedes        = []
blocks            = []
caused_by         = []
see_also          = []
+++

### RLG-327 — The tyre marks are drawn off the bottom of the screen, and the smoke is dead code
- **RULED 2026-09-22T21:06:51-04:00** — OWNER, 2026-09-22: "We also want to doctor and remove any dead code. I think once upon a time we implemented tire marks but I haven't seen them ever. Tire smoke too."

BOTH HALVES ARE TRUE AND THEY FAIL FOR DIFFERENT REASONS. Proved by `tools/rubber-proof.py`, recorded as EVD-1296, five assertions passing.

THE SMOKE IS DEAD CODE, PLAINLY. `tyreSmoke` is declared, aged every frame by `stepRubber`, drawn every frame by `drawRubber`, and capped by a constant of its own - and NOTHING HAS EVER PUSHED INTO IT. `layRubber` carries the comment that removed it: the tyre smoke fought with the damage smoke coming off the bonnet for the same patch of screen and neither read clearly. The decision was right and the code was left behind. After the hardest scrubbing the harness can produce, the array holds nothing.

THE MARKS ARE LAID, AGED, PROJECTED, FILLED - AND MISS THE FRAME. They are laid at `pos + PLAYER_Z - 340`, which is 340 units BEHIND the player's own car, and the road behind your own wheels is below the bottom edge of a forward-facing view. Measured on a 480x862 canvas, sampled on the frame where the most marks were drawn: 144 marks on the road, 114 thrown away as behind the car, 30 projected and filled, and they land between y=1085 and y=12036. THE FRAME ENDS AT 862. NOT ONE MARK HAS EVER BEEN ON SCREEN.

A FIRST HYPOTHESIS WAS WRONG AND IS RECORDED BECAUSE IT WAS PLAUSIBLE AND BECAUSE IT WOULD HAVE COST A WASTED FIX. It looked like a draw-order fault: `drawRubber` is the first thing `drawWorld` paints, `drawRoad` paints the tarmac afterwards, so the marks appeared to be buried under the road surface. That is TRUE of the draw order and IRRELEVANT to the bug - the marks are off the bottom of the frame before the road ever covers them. Reordering the passes would have changed nothing and would have looked like a fix. A screenshot with the tarmac lifted showed no marks either, and that is what sent this back to the geometry.

SO A FORWARD VIEW IS THE WRONG PLACE FOR A TYRE MARK. What is behind the car is what the MIRROR shows, and `drawRubber` is called only from `drawWorld`, which is the front pass. The glass has no rubber pass at all. THIS IS THE SAME SHAPE AS [[RLG-326]]: the things that belong behind you - your own smoke, your own rubber - are drawn only where they cannot be seen, and are absent from the one view that would show them.

AND IT COSTS A FRAME SOMETHING FOR NOTHING. Thirty marks a frame is thirty projections and ninety fill calls, every one of them off-screen. The cap is 180 marks, so a long slide pays three times that. It was never measured by the layer sweep in [[RLG-299]] because it had no switch; it has one now, `layerOff('rubber')`, added for this proof.

WHAT THE OWNER HAS NOT BEEN ASKED YET, and it is one question rather than three: whether the rubber is DELETED with the smoke, or MOVED to the glass where it would be seen. The second is more work and would put marks in the one view a player checks when the back of the car is loose. The first is free and honest. Nothing is built either way.
- **RULED 2026-09-22T21:26:57-04:00** — BUILT 2026-09-22, owner's ruling the same day: delete both, with the smoke. "Delete both, with the smoke" - the marks can be rebuilt in the glass later as their own piece of work, and nothing is lost but code that never showed.

WHAT WENT: the two arrays, the cap, `layRubber`, `stepRubber`, `drawRubber` and its call, and the six places that laid a mark - the player's steering and braking, a gear-change kick, a launch, a wheelspin, the rivals and the police cruisers.

AND THE DELETION BROKE THE TYRE SQUEAL ON THE WAY THROUGH, WHICH IS THE PART WORTH KEEPING. `scrubOf` measures how hard a car is sliding across the road, and every one of its three callers fed a tyre mark - so it read as part of the rubber and went out with it. It is not. `step()` is a 2,800-line function, and it reads the player's own scrub 2,300 lines BELOW where the value is computed, then hands it to the squeal. Removing the computation left a reference to a name that no longer existed, and:

  - `node --check road.js` PASSED. It is a reference error, not a syntax one.
  - `pack.sh --check` PASSED, on all seven of its checks.
  - `squeal-test.py` WOULD HAVE PASSED. It pins the car on its line so the CORNER load is the only cause, and it says so in its own header: "a car carried sideways squeals by the older path". The older path is the one that was being deleted, and nothing covered it.

It was caught by grepping for each deleted name before trusting the parser, which is the only reason it is not in the build. THE LESSON IS THE PROJECT'S OWN AND IT HELD AGAIN: a green build is not evidence.

SO THE GAP IS CLOSED RATHER THAN NOTED. `tools/scrub-squeal-test.py` drives a STRAIGHT road, where no corner load can be the cause, and asserts that holding it steady is silent and sawing the wheel squeals. It reads the audio layer's own gain through `API.squealLevel`, not `scrubOf`'s return value - a check on the cause would pass with the wire to the sound cut, which is the exact failure it exists for. IT WAS WATCHED FAILING: with the scrub cut out of the squeal line the sawed level reads 0.0000 and two of its three assertions fail; restored, it reads 0.0352.

AND THE VERSION STAMP WAS CAUGHT BY ITS OWN CHECK. `A.version` was bumped to 0.14.128 in the previous commit and `window.ROAD_BUILD` was not, so 507ede5e shipped a MIXED build tag. smoke-test's stamps check found it here and both machines now read 0.14.128. The previous commit is left standing rather than rewritten - the history is public and is never rewritten - and this one corrects it.

WHAT IS GONE THAT WAS NEVER THERE. The record at the spin timer said the rubber was "the only thing on the screen that says why it is not moving". It never was on the screen, so a spinning car has never had that tell and removing the marks takes nothing from it. Whatever replaces it has to be in front of the car or in the glass.

THE PROOF THAT JUSTIFIED THIS IS GONE WITH ITS SUBJECT. `tools/rubber-proof.py` asserted behaviour that no longer exists, so it is deleted; EVD-1296 keeps its result permanently.
