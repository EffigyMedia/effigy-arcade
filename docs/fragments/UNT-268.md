+++
id                = "UNT-268"
kind              = "unit"
status            = "closed"
cost              = 76793
closed            = "2026-09-12T04:40:56-04:00"
created           = "2026-09-12T03:21:39-04:00"
created_precision = "second"
supersedes        = []
blocks            = []
caused_by         = ["RLG-213", "RLG-100"]
see_also          = []
delegate          = false
effort            = "high"
model             = "claude-opus-5"
ran_on            = "claude-opus-5"
routing           = "record"
+++

### UNT-268 - Measure how far a timed test drive can actually reach before choosing the unlock distance

- Routing class: `record` -> **claude-opus-5**, effort **high**. Bias 1. No class given, or one the table does not name. ([[RLG-092]])
- Opened for: [[RLG-213]], [[RLG-100]]
- Opened by `unit.py`. **The identity exists before the first edit** ([[RLG-097]]), so the snapshot has somewhere to go.
- **CLOSED 2026-09-12T04:40:56-04:00** - The owner asked whether the merged unlock should be 50 miles or 100, and confirmed miles in one run. The measurement says neither is reachable, and neither is the 50 that is already in the code. tools/reach-test.py drives a car flat out with the timer on until the clock ends the run: nine runs across three cars ended between 1.39 and 9.23 miles, every one of them with the clock at zero. A checkpoint is every two miles and pays twenty seconds, so breaking even needs 360mph and the fastest car declares 194 - the clock drains on every mile of every run and a crate's ten seconds is the only push back. RLG-215 records that, with three ways out for the owner to pick from. A second defect was found on the way: the DEBUG switches list a car they will not let you drive, because openBy widens for them and carLocked does not, and it fails silently with the run sitting on the menu. RLG-214 records that. The harness carries a falsifier that pins the speed and shows it can report a run twice past the measured ceiling with the clock climbing, and it states in its own output what that does not prove.
