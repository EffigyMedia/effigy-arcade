+++
id                = "EVD-889"
kind              = "evidence"
status            = "recorded"
step              = "trap-switch-test passes after the parked-trap steering fix"
unit              = "UNT-325"
command           = "P:\\Code_Continuum\\Projects\\In-Dev\\Effigy_Arcade\\.venv\\Scripts\\python.exe tools/trap-switch-test.py"
exit_code         = 0
duration_ms       = 59656
output_bytes      = 1000
output_sha        = "0cb0fb723689d31f7924ec35b575edd0"
store_stamp       = "63d410681850a9dd"
created           = "2026-09-15T08:20:04-04:00"
created_precision = "second"
supersedes        = []
blocks            = []
caused_by         = []
see_also          = []
+++

### EVD-889 — step `trap-switch-test passes after the parked-trap steering fix` of [[UNT-325]]

- Ran: `P:\Code_Continuum\Projects\In-Dev\Effigy_Arcade\.venv\Scripts\python.exe tools/trap-switch-test.py`
- Result: **exit 0**, in 59656 ms
- Output: 1000 bytes, sha256 `0cb0fb723689d31f7924ec35b575edd0`
- Store stamp at the time: `63d410681850a9dd`

**Output** — head only; the digest above covers the whole of it, so this excerpt cannot be selective without saying so.

```
trap-switch  .  speed traps ignore the player by default, and the debug switch restores them
      serving P:\Code_Continuum\Projects\In-Dev\Effigy_Arcade
  browser: installed chrome - no bundled chromium, so the engine version is whatever this machine has
  ok    the debug menu boots with SPEED TRAPS ENGAGE OFF
  ok    tapping it shows ON, and the engine reads ON
  ok    tapping again shows OFF
      DEFAULT   engaged at dz []; each pass (heat points gained, Interceptors earned) [(20, True), (10, True), (10, True)]
  ok    with the switch as it boots, no trap driven past at speed engages the player
  ok    but each pass still adds the trap's heat, once
  ok    and a pass over 170 still earns the Interceptors
      RESTORED  engaged at dz [-9, 1, -23] (positive is up the road)
  ok    with the switch ON, traps driven past at speed engage
  ok    and each engages at or behind the player
  ok    no page errors

speed traps ignore the player by default, and the debug switch restores them
```

*Written by `step.py` as a byproduct of running the step. **Nobody attested to this** ([[RLG-081]]).*
