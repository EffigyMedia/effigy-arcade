+++
id                = "EVD-509"
kind              = "evidence"
status            = "recorded"
step              = "FALSIFICATION: with one width for all six cars again, the per-body checks and the spread must go red"
unit              = "UNT-205"
command           = "P:/Code_Continuum/Projects/In-Dev/Effigy_Arcade/.venv/Scripts/python.exe tools/collide-test.py"
exit_code         = 1
duration_ms       = 51203
output_bytes      = 1341
output_sha        = "53a08e76dc3cc2dc30d99b33471c4489"
store_stamp       = "edb32ddeee54652f"
created           = "2026-09-08T04:24:34-04:00"
created_precision = "second"
supersedes        = []
blocks            = []
caused_by         = []
see_also          = []
+++

### EVD-509 — step `FALSIFICATION: with one width for all six cars again, the per-body checks and the spread must go red` of [[UNT-205]]

- Ran: `P:/Code_Continuum/Projects/In-Dev/Effigy_Arcade/.venv/Scripts/python.exe tools/collide-test.py`
- Result: **EXIT 1 — the step FAILED**, in 51203 ms
- Output: 1341 bytes, sha256 `53a08e76dc3cc2dc30d99b33471c4489`
- Store stamp at the time: `edb32ddeee54652f`

**Output** — head only; the digest above covers the whole of it, so this excerpt cannot be selective without saying so.

```
collide-test  .  the hit is where the car is
  browser: playwright bundled chromium (pinned)
  ok    the engine can park a car and report the damage
      parked a sedan: half-widths sum to 0.22304 of the road
  ok    a car in the same place as you is a hit
  ok    and one well clear of you is not
      the hit stops at a lateral offset of 0.2230
  ok    a hit fires when the cars overlap and stops when they do not
  ok    and it stops exactly at the sum of the two half-widths
      truck  (0.320 wide) collides at 0.2416
      coupe  (0.260 wide) collides at 0.2169
      the player therefore collides at 0.2648 of the road, drawn at 0.2650
      (diagnostic, not an assertion: the separation puts the player at 0.2648 against a drawn 0.2650 - see the note in the source)
  ok    the staged collision survives a change of car
      ROADSTER  hit edge 0.2230 against a declared sum of 0.2230 (its own width 0.2650)
  ok    the roadster collides at its own declared width
  ok    the staged collision survives a change of car
      MUSCLE    hit edge 0.2230 against a declared sum of 0.2230 (its own width 0.2650)
  ok    the muscle collides at its own declared width
  FAIL  and two different cars are two different widths   [edges 0.2230 and 0.2230, 0.0000 apart against about 0.0000 expected]
  ok    no page errors

1 check(s) failed
```

*Written by `step.py` as a byproduct of running the step. **Nobody attested to this** ([[RLG-081]]).*
