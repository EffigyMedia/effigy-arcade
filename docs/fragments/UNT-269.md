+++
id                = "UNT-269"
kind              = "unit"
status            = "closed"
cost              = 20828
closed            = "2026-09-12T08:59:29-04:00"
created           = "2026-09-12T08:45:51-04:00"
created_precision = "second"
supersedes        = []
blocks            = []
caused_by         = ["RLG-215"]
see_also          = []
delegate          = false
effort            = "high"
model             = "claude-opus-5"
ran_on            = "claude-opus-5"
routing           = "record"
+++

### UNT-269 - Withdraw the unreachable-unlock finding: the harness never steered and never collected a crate

- Routing class: `record` -> **claude-opus-5**, effort **high**. Bias 1. No class given, or one the table does not name. ([[RLG-092]])
- Opened for: [[RLG-215]]
- Opened by `unit.py`. **The identity exists before the first edit** ([[RLG-097]]), so the snapshot has somewhere to go.
- **CLOSED 2026-09-12T08:59:29-04:00** - The owner reported driving over 100 miles in one run, repeatedly, which contradicts the measurement committed an hour earlier. They are right. RLG-215 is withdrawn: the harness held the throttle and never touched the wheel, so it never collected a crate - crates are parked on the shoulder and the engine says so - and it never reached the car's declared speed. Both terms belong to the driver. The budget is v/360 plus the crate rate minus one, and at the top of the fleet with the crates taken the clock climbs and the run is endless. The nine runs measured a floor and nothing else. The harness now says so in its own name, its output and its docstring, and driver_limits prints FLOOR ONLY with the reason beside every distance. Its selftest proves that check fires on a bad driver and stays silent on a good one. RLG-213 carries a correction so the resort is not left reading a block that does not exist. RLG-214 is unaffected.
