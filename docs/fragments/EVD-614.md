+++
id                = "EVD-614"
kind              = "evidence"
status            = "recorded"
step              = "the car never reaches the speed the check asks for and its headroom over the gate collapses; held every frame it reads a flat 160, and held BELOW the gate it still sends nothing"
unit              = "UNT-244"
command           = "P:/Code_Continuum/Projects/In-Dev/Effigy_Arcade/.venv/Scripts/python.exe tools/super-gate-proof.py"
exit_code         = 1
duration_ms       = 69486
output_bytes      = 1182
output_sha        = "98db93d11f46cc2a2530df4024fd7372"
store_stamp       = "6eda78ecbf7f8b82"
created           = "2026-09-09T22:02:55-04:00"
created_precision = "second"
supersedes        = []
blocks            = []
caused_by         = []
see_also          = []
+++

### EVD-614 — step `the car never reaches the speed the check asks for and its headroom over the gate collapses; held every frame it reads a flat 160, and held BELOW the gate it still sends nothing` of [[UNT-244]]

- Ran: `P:/Code_Continuum/Projects/In-Dev/Effigy_Arcade/.venv/Scripts/python.exe tools/super-gate-proof.py`
- Result: **EXIT 1 — the step FAILED**, in 69486 ms
- Output: 1182 bytes, sha256 `98db93d11f46cc2a2530df4024fd7372`
- Store stamp at the time: `6eda78ecbf7f8b82`

**Output** — head only; the digest above covers the whole of it, so this excerpt cannot be selective without saying so.

```
super-gate-proof  .  the check read zero because the car was not going fast enough
  browser: playwright bundled chromium (pinned)
      the rule under test: above 150mph for 4 unbroken seconds, at heat 3 or more
      OLD  asked for 160mph: the car read 140-167mph, under the gate on 2 of 64 samples; the longest unbroken stretch banked 7.07s of the 4s needed; 4 supers
  FAIL  a speed asked for four times a second is never actually reached   [the car got to 167mph, so the sag this is about is not happening]
  ok    and the headroom the check thought it had over the gate is gone
      NEW  asked for 160mph: the car read 160-160mph, under the gate on 0 of 64 samples; the longest unbroken stretch banked 11.87s of the 4s needed; 4 supers
  ok    holding it keeps the car above the gate on every sample
  ok    so the unbroken seconds the rule asks for are actually banked
  ok    and an interceptor is dispatched
      SLOW asked for 140mph: the car read 140-140mph, under the gate on 64 of 64 samples; the longest unbroken stretch banked 0.00s of the 4s needed; 0 supers
  ok    below the gate, held just as steadily, nothing is sent
  ok    no page errors

1 check(s) failed
```

*Written by `step.py` as a byproduct of running the step. **Nobody attested to this** ([[RLG-081]]).*
