+++
id                = "EVD-1271"
kind              = "evidence"
status            = "recorded"
step              = "race-test with the rivals in full livery"
unit              = "UNT-419"
command           = "P:\\Code_Continuum\\Projects\\In-Dev\\Effigy_Arcade\\.venv\\Scripts\\python.exe tools/race-test.py"
exit_code         = 0
duration_ms       = 53148
output_bytes      = 944
output_sha        = "33da0b98a9a7c13b5388867a254254d6"
store_stamp       = "0c6c8660c3ec3cb1"
created           = "2026-09-22T11:17:20-04:00"
created_precision = "second"
supersedes        = []
blocks            = []
caused_by         = []
see_also          = []
+++

### EVD-1271 — step `race-test with the rivals in full livery` of [[UNT-419]]

- Ran: `P:\Code_Continuum\Projects\In-Dev\Effigy_Arcade\.venv\Scripts\python.exe tools/race-test.py`
- Result: **exit 0**, in 53148 ms
- Output: 944 bytes, sha256 `33da0b98a9a7c13b5388867a254254d6`
- Store stamp at the time: `0c6c8660c3ec3cb1`

**Output** — head only; the digest above covers the whole of it, so this excerpt cannot be selective without saying so.

```
race-test  .  a race has checkpoints, whatever the toggle says
  browser: installed chrome - no bundled chromium, so the engine version is whatever this machine has - GPU OFF (EFFIGY_NO_GPU), so no frame rate here is comparable
  ok    the garage starts on TEST DRIVE
  ok    and TIMED can be turned off there
  ok    and back on again
  ok    then the mode changes to a race
      TIMED off then on, then a race: 1 board(s)
  ok    a race puts checkpoint boards on the road
  ok    TIMED can be left off
  ok    with the mode set to a race
      TIMED left OFF, then a race: 1 board(s), clock at 102.4
  ok    a race with TIMED off still puts boards on the road
      the checkpoint flash reads: 'CHECKPOINT  +20 SEC'
  ok    a checkpoint is actually crossed
  ok    and it names a unit rather than a bare number
      it awarded 20 SEC
  ok    and the unit is a word and the award is a real number
  ok    no page errors

  all checks passed
```

*Written by `step.py` as a byproduct of running the step. **Nobody attested to this** ([[RLG-081]]).*
