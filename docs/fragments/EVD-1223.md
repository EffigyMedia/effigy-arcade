+++
id                = "EVD-1223"
kind              = "evidence"
status            = "recorded"
step              = "retry-test after the RLG-322 fix"
unit              = "UNT-415"
command           = "P:\\Code_Continuum\\Projects\\In-Dev\\Effigy_Arcade\\.venv\\Scripts\\python.exe tools/retry-test.py"
exit_code         = 0
duration_ms       = 8865
output_bytes      = 1637
output_sha        = "b41fb95730a3b823ceb435a32242ef0e"
store_stamp       = "305b0356d32a9356"
created           = "2026-09-22T09:30:17-04:00"
created_precision = "second"
supersedes        = []
blocks            = []
caused_by         = []
see_also          = []
+++

### EVD-1223 — step `retry-test after the RLG-322 fix` of [[UNT-415]]

- Ran: `P:\Code_Continuum\Projects\In-Dev\Effigy_Arcade\.venv\Scripts\python.exe tools/retry-test.py`
- Result: **exit 0**, in 8865 ms
- Output: 1637 bytes, sha256 `b41fb95730a3b823ceb435a32242ef0e`
- Store stamp at the time: `305b0356d32a9356`

**Output** — head only; the digest above covers the whole of it, so this excerpt cannot be selective without saying so.

```
retry-test  .  a retry starts a new run, not the last one continued
  browser: installed chrome - no bundled chromium, so the engine version is whatever this machine has - GPU OFF (EFFIGY_NO_GPU), so no frame rate here is comparable
      the world was dirtied to: {'wet': 0.8, 'wetTarget': 0.8, 'snowy': 1, 'settle': 0.91, 'cloud': 0.947, 'storm': 0.9287035855305115, 'pool': 0.995, 'grip': 0.34, 'biome': 'FOREST', 'from': 'FOREST'
  ok    the world really is snowy and stormy before the retry
  ok    and the road really is soaked, with the grip to show for it
      at the instant of the reset: settle 0, pool 0, opening in CITY (its own floor 0)
      and the next run starts at: {'wet': 0, 'wetTarget': 0, 'snowy': 0, 'settle': 0, 'cloud': 0.477, 'storm': 0, 'pool': 0, 'grip': 1, 'biome': 'CITY', 'from': 'CITY', 'to': 'CITY', 'wetIn': 43.4, 'c
  ok    a retry starts with nothing settled on the road
  ok    and with nothing standing on it either
  ok    and no thunder is left over from the run before
  ok    and the weather it starts in is the weather it rolled
  ok    and nothing is due the moment it starts
  ok    and the run before it left nothing behind
  ok    and it starts in one place rather than part-way between two

      the same checks, with the two resets taken back out
      it starts at: pool 0.938, grip 0.756
  ok    without the reset the water comes across, so the check was measuring it
  ok    no page errors

  all checks passed
```

*Written by `step.py` as a byproduct of running the step. **Nobody attested to this** ([[RLG-081]]).*
