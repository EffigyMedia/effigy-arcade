+++
id                = "EVD-922"
kind              = "evidence"
status            = "recorded"
step              = "leaving a run now stops every sound it was making, and the check still fails when the old engine idle is put back"
unit              = "UNT-334"
command           = ".venv\\Scripts\\python tools/run-silence-test.py"
exit_code         = 0
duration_ms       = 20061
output_bytes      = 942
output_sha        = "29e45702a52849ab0a78cc819988ab62"
store_stamp       = "8ddd6bd8c3fb174f"
created           = "2026-09-16T02:28:57-04:00"
created_precision = "second"
supersedes        = []
blocks            = []
caused_by         = []
see_also          = []
+++

### EVD-922 — step `leaving a run now stops every sound it was making, and the check still fails when the old engine idle is put back` of [[UNT-334]]

- Ran: `.venv\Scripts\python tools/run-silence-test.py`
- Result: **exit 0**, in 20061 ms
- Output: 942 bytes, sha256 `29e45702a52849ab0a78cc819988ab62`
- Store stamp at the time: `8ddd6bd8c3fb174f`

**Output** — head only; the digest above covers the whole of it, so this excerpt cannot be selective without saying so.

```
run-silence  .  leaving a run stops every sound the run was making
      serving P:\Code_Continuum\Projects\In-Dev\Effigy_Arcade
  browser: installed chrome - no bundled chromium, so the engine version is whatever this machine has
      DRIVING          ctx running  sounding eng 0.0899, eng2 0.0440, wind 0.0242
  ok    the audio context is live
  ok    the engine is actually sounding while you drive, so silence means something
  ok    the run ends on the clock and the end card comes up
      END CARD         ctx running  sounding eng 0.0001
  ok    the context is still live under the end card
  ok    every sound the run was making has stopped under the end card
      STILL, LATER     ctx running  silent
  ok    and it is still silent four seconds on
      GARAGE           ctx running  silent
  ok    and going back to the garage does not start it up again
  ok    no page errors

leaving a run stops every sound the run was making
```

*Written by `step.py` as a byproduct of running the step. **Nobody attested to this** ([[RLG-081]]).*
