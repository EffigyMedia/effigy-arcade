+++
id                = "UNT-295"
kind              = "unit"
status            = "closed"
cost              = 45531
closed            = "2026-09-13T00:26:49-04:00"
created           = "2026-09-13T00:04:22-04:00"
created_precision = "second"
supersedes        = []
blocks            = []
caused_by         = []
see_also          = []
delegate          = false
effort            = "high"
model             = "claude-opus-5"
ran_on            = "claude-opus-5"
routing           = "change"
+++

### UNT-295 - A checkpoint pays by class: production 30, sports 20, super 10

- Routing class: `change` -> **claude-opus-5**, effort **high**. Bias 1. A policy or vocabulary change reaches every file that follows it. ([[RLG-092]])
- Opened by `unit.py`. **The identity exists before the first edit** ([[RLG-097]]), so the snapshot has somewhere to go.
- **CLOSED 2026-09-13T00:26:49-04:00** - v0.14.9. The owner's numbers: production 30 seconds a checkpoint, sports 20, super 10. A supercar covers the two miles to the next gantry in a fraction of the time a saloon needs, so a flat twenty was worth far more to the fast car and the run got EASIER as the ladder went up. CP_SECONDS is a table keyed on class and CLOCK_BONUS stays as the DEFAULT rather than a fourth number, so a class with no row is paid the twenty it always was. The award and the number on the flash are read once together, because a gantry that pays ten and says twenty is worse than either being wrong alone. THE POLICE ARE DERIVED AND NOT LISTED - a CRUISER declares raceClass sports and a SUPERCRUISER declares super, so they take 20 and 10 through the record and a third force car would need no edit. THE FIRST BUILD ASKED classOf AND WOULD HAVE PAID A VAN TEN SECONDS: that function falls through to super for every body outside its three lists and the utility vehicles are in none of them, so the slowest thing in the game would have had the supercar's reward. It was caught by writing the test's expectations down before running it. FORMULA AND UTILITY ARE NOT IN THE TABLE and the owner has not ruled on them - both hold the default and both must be asked about rather than guessed. tools/checkpoint-pay-test.py measures THE CLOCK at a real gantry rather than the table, and --falsify empties the table so the four cars whose award changed fail while the four that read twenty either way pass. THE HARNESS TOOK THREE GOES and the reasons are in the fragment: a board is only laid within 90,000 units and the first sits at 257,400; nextCP only ever moves forward so jumping a later car BACK lays nothing and the check went blind on a different car each run; and a gain of zero is the instrument missing rather than the engine paying nothing. AND RLG-100 COULD NOT BE RE-MEASURED - verify-097-100.py was run as it should have been and reported three green checks against three cars that never moved, 0.00 miles and 0 mph. That is RLG-234, and RLG-100 stays open with its last real readings dated 2026-09-08.
