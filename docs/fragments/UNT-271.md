+++
id                = "UNT-271"
kind              = "unit"
status            = "closed"
cost              = 41101
closed            = "2026-09-12T09:36:06-04:00"
created           = "2026-09-12T09:24:27-04:00"
created_precision = "second"
supersedes        = []
blocks            = []
caused_by         = ["RLG-210"]
see_also          = []
delegate          = false
effort            = "medium"
model             = "claude-sonnet-5"
ran_on            = "claude-opus-5"
routing           = "feature"
+++

### UNT-271 - The garage card shows one end, large, centred and floor-anchored, with a corner button to flip it

- Routing class: `feature` -> **claude-sonnet-5**, effort **medium**. Bias 1. Implementation to a spec that already answers the design questions. ([[RLG-092]])
- Opened for: [[RLG-210]]
- Opened by `unit.py`. **The identity exists before the first edit** ([[RLG-097]]), so the snapshot has somewhere to go.
- **CLOSED 2026-09-12T09:36:06-04:00** - RLG-210 built, v0.13.86, all five parts including the flip button the owner brought forward on 2026-09-12. The card shows one end, drawn into the whole 300 rather than half of it, centred, on the floor it already stood on, in the two-tier box it already reserved. The fit is still taken across BOTH ends, because a scale that followed the shown end would resize the car on every flip, which is what RLG-182 fixed. The flip is a chip in the top right that says where it will take you rather than where you are, it is on the locked card too, and the visible chip is small with a 44-pixel touch target laid over it - padding the chip itself to 44 was built and photographed first and read as a panel. The front is restored on ENTRY rather than on every redraw, because showGarage is what every control in that screen calls to redraw. The proof's first version passed with the defect present: asking whether the middle of the card was painted no longer separated the two layouts once the fit was widened, and only putting the old layout back revealed it. It measures against the engine's own fit now and was watched failing in both directions.
