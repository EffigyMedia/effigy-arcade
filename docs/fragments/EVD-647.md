+++
id                = "EVD-647"
kind              = "evidence"
status            = "recorded"
step              = "how far one timed test drive reaches, three runs a car, with a falsifier that proves the harness can report a hundred-mile run"
unit              = "UNT-268"
command           = "P:\\Code_Continuum\\Projects\\In-Dev\\Effigy_Arcade\\.venv\\Scripts\\python.exe tools/reach-test.py"
exit_code         = 1
duration_ms       = 1337284
output_bytes      = 1626
output_sha        = "e0f4c66fdf3b1fd165bd81f15ca4e746"
store_stamp       = "ecd0d8c28f0f90ed"
created           = "2026-09-12T04:06:47-04:00"
created_precision = "second"
supersedes        = []
blocks            = []
caused_by         = []
see_also          = []
+++

### EVD-647 — step `how far one timed test drive reaches, three runs a car, with a falsifier that proves the harness can report a hundred-mile run` of [[UNT-268]]

- Ran: `P:\Code_Continuum\Projects\In-Dev\Effigy_Arcade\.venv\Scripts\python.exe tools/reach-test.py`
- Result: **EXIT 1 — the step FAILED**, in 1337284 ms
- Output: 1626 bytes, sha256 `e0f4c66fdf3b1fd165bd81f15ca4e746`
- Store stamp at the time: `ecd0d8c28f0f90ed`

**Output** — head only; the digest above covers the whole of it, so this excerpt cannot be selective without saying so.

```
reach  .  how far ONE timed test drive gets before the clock ends it
  browser: installed chrome - no bundled chromium, so the engine version is whatever this machine has

  FALSIFIER - can this harness report a LONG run at all?
      TUNER with the speed pinned at 10,000mph, timer ON
        15.72 miles in 90s of wall clock, clock 60.0 -> 122.7 (lowest 49.9)
  FAIL  the harness can report a run past 100 miles, so a short answer below is the game

  HOW FAR DOES A REAL CAR GET, FLAT OUT, TIMER ON
      checkpoints are 2 miles apart and pay 20s, a crate pays 10s,
      and the run starts with 60s on the clock
      SALOON     BLKD  the run never started - peak speed was zero, so this car was not driveable
      SALOON     BLKD  the run never started - peak speed was zero, so this car was not driveable
      SALOON     BLKD  the run never started - peak speed was zero, so this car was not driveable
      TUNER        3.19 miles   the clock ran out after  82s   0 crate(s), peak 146 mph
      TUNER        5.24 miles   the clock ran out after 122s   2 crate(s), peak 135 mph
      TUNER        5.81 miles   the clock ran out after 102s   0 crate(s), peak 146 mph
                   -> 3.19 to 5.81 miles over 3 runs
      MUSCLE       2.50 miles   the clock ran out after  92s   1 crate(s), peak 124 mph
      MUSCLE       5.09 miles   the clock ran out after 112s   1 crate(s), peak 143 mph
      MUSCLE       5.37 miles   the clock ran out after 142s   4 crate(s), peak 121 mph
                   -> 2.50 to 5.37 miles over 3 runs

FAILED: the falsifier did not clear 100 miles - every number below is unreadable
```

*Written by `step.py` as a byproduct of running the step. **Nobody attested to this** ([[RLG-081]]).*
