+++
id                = "UNT-293"
kind              = "unit"
status            = "closed"
cost              = 54246
closed            = "2026-09-12T23:49:35-04:00"
created           = "2026-09-12T23:22:59-04:00"
created_precision = "second"
supersedes        = []
blocks            = []
caused_by         = ["RLG-182"]
see_also          = []
delegate          = false
effort            = "medium"
model             = "claude-sonnet-5"
ran_on            = "claude-opus-5"
routing           = "fix"
+++

### UNT-293 - The garage gives the car a floor to stand on, so its tyres read

- Routing class: `fix` -> **claude-sonnet-5**, effort **medium**. Bias 1. A known defect with a known cause. ([[RLG-092]])
- Opened for: [[RLG-182]]
- Opened by `unit.py`. **The identity exists before the first edit** ([[RLG-097]]), so the snapshot has somewhere to go.
- **CLOSED 2026-09-12T23:49:35-04:00** - v0.14.7. The owner asked on 2026-09-09 to see a little bit of the tyres at the bottom of the garage card, and the record said there were no wheels in the sprite to show. THAT WAS WRONG AND IT WAS WRITTEN FROM THE SILHOUETTE - the one drawing in the game with its detail deliberately removed, so of course it has no wheels. Measured off the front sprite: a SALOON's wheels are rows 152 to 154, two blocks of 36 pixels, at luminance 16 to 22, with groundShadow's full-width bar at 50 per cent black across rows 156 to 162 directly beneath them. A near-black tyre, above a black shadow, on a near-black card. Nothing was missing and nothing was visible. THE ROOM IS LIT RATHER THAN THE CAR REPAINTED, for two reasons that both bite: lightening a tyre would lighten it on the road where it is correct, and RLG-184 declares the outline once for both ends so anything added under the body at one end changes both by construction. The fault is only in this card so the fix is only in this card. AND IT IS CSS RATHER THAN PAINT ON THE CARD CANVAS, which is not a style choice - it was drawn on the canvas for one build and broke garage-card-test, whose width checks count non-transparent pixels to answer how wide the car is: three cars went from 224 to 266 pixels to a flat 287, the width of the floor. A check that cannot tell the car from the scenery is the check being right, so the canvas stays the car and the floor is a .gwrap::before with drawGarageCar writing only --gfloor. Both cabinets got the rule, which is RLG-226's lesson from four hours earlier. THE PROOF'S FIRST VERSION PASSED ITS OWN FALSIFIER: the floor check's control read the card's outer edges, which are dark either way. Measured against the floor's own columns 70 pixels higher it is 71.9 against 11.6 with the floor and 20.2 against 11.6 without, so the card's own gradient is worth 8.6 and had been clearing a threshold of 6. The bar is 30 now. AND THE HARNESS HAD BEEN RED AGAINST A PRODUCT THAT WAS RIGHT - it walked TUNER, MUSCLE and ROADSTER, all sports, which RLG-213 locked on a fresh save, so every card it measured was a locked one and three flip-button checks failed every run because a locked card correctly has no flip button. It walks SALOON, COUPE and HATCH now and four checks that had never been reached run for the first time.
