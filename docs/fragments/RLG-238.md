+++
id                = "RLG-238"
kind              = "ruling"
type              = "defect"
status            = "in-flight"
created           = "2026-09-13T13:33:16-04:00"
created_precision = "second"
source            = "owner, 2026-09-13"
supersedes        = []
blocks            = []
caused_by         = []
see_also          = ["RLG-237", "RLG-153"]
+++

### RLG-238 — The road ahead shows through a tunnel wall
- **RULED 2026-09-13T13:34:14-04:00** — **REPORTED 2026-09-13, from the device, and NOT STARTED.** Owner: *"The future roadway is shown through the tunnel wall."* It arrived while [[RLG-237]] was being built, so it may describe build 0.14.10, before the tube was moved to start at the camera.

**NOT REPRODUCED, AND NOT TO BE DIAGNOSED FROM A GUESS.** A harness drove a tunnel with its bend set to 1.0 and took six screenshots on build 0.14.12. Two things looked wrong at the far end, and neither is certainly what the owner means:
- the dark depth-fade blocks at the far end are rectangles, and on a bend they jut sideways past the tube;
- on a bend, a red kerb wedge is painted over the lower left wall.

A third candidate was not seen but follows from the code: the tube is 16 samples joined by straight lines, spaced by the square of the distance, so the last spans are thousands of units long. On a bend the road between two far samples can curve outside the straight wall between them.

**NEXT:** ask the owner for a screenshot on 0.14.12 or later, then measure against the screenshot. See [[RLG-153]], the open tunnel complaint.
- **RULED 2026-09-13T14:03:25-04:00** — **DIAGNOSED AND BUILT 2026-09-13, v0.14.14. NOT SIGNED** ([[RLG-116]]).

**THE OWNER CORRECTED ME TWICE, AND WAS RIGHT BOTH TIMES.** I recorded this as not reproduced and asked for a screenshot. Owner: *"Every one of those tunnel pictures has the offending issue clearly shown."* My own six captures of a winding tunnel did show it. After the first fix I called the remaining black blocks a separate artefact. Owner: *"No it's still clearly happening in those pictures. The black showing THROUGH the tunnel walls."* It was the same fault in a second pass.

## TWO WRONG EXPLANATIONS WERE MEASURED AND DROPPED

1. **Sparse samples.** The tube is 16 samples spaced by the square of the distance, so I expected the road to bend outside the straight wall between two far samples. A probe (`boreLeak`, since replaced) measured that across 132 road segments: 1.2 pixels at worst. Not the cause.
2. Before that, in [[RLG-237]], the camera leaving the bore. Also measured wrong.

## THE CAUSE: THE FILL RULE, IN TWO PLACES

**1. THE ROAD.** Each tube surface was ONE polygon: along its lower edge from near to far, then back along its upper edge. On a bend the wall's screen x runs one way and then the other, so the polygon folds over itself. Canvas fills with the nonzero rule, under which the two layers of a fold have opposite winding and cancel. The fold is exactly where a nearer stretch of wall stands between the eye and the road beyond the bend, so that stretch was a hole. **`boreStrip` now builds every surface (vault halves, wall faces, outer surfaces, barrier) span by span, winding every quad the same way before it goes into one path**, so overlaps add. One `ctx.fill` per surface, as before.

**2. THE BLACK.** `drawBoreDepth` paints the far end's darkness AFTER the walls, so nothing hid it. On a bend the darkness round the corner was laid straight over the nearer wall. **Each dark quad is now clipped to the screen less every nearer wall-face span**, added as the loop walks outward, so each span is clipped once a frame.

## THE PROOF

`tools/tunnel-bend-test.py`, new, on a tunnel with its bend forced to 1.0 and traffic cleared on every poll.
- `API.boreOcclusion`: road points that a nearer wall span hides in the geometry are put to `isPointInPath` against the paths the last frame filled, under the browser's real fill rule.
- `API.boreDarkThrough`: points in the last three dark quads that a nearer wall span hides are read back off the canvas pixels. The wall is pale, so near-black is the darkness.

| Run | Road hidden / showing through | Darkness hidden / reading black | Evidence |
|---|---|---|---|
| fixed | 197 / 0 | 93 / **6** - FAILED | EVD-758 |
| fixed | 441 / 0 | 114 / 0 | EVD-761 |
| `--falsify winding` | 303 / 118 | - | EVD-759, only the road check failed |
| `--falsify dark` | - | 1355 / 356 | EVD-760, only the darkness check failed |

**THE ONE FAILED RUN IS NOT EXPLAINED.** Before traffic was cleared, 9 of 1055 points read black and they were cars' dark glass drawn over the walls. After it was cleared, one run in six had 6 of 93 black, on a short tunnel where the exit came into range. Five other runs, including four unrecorded ones, read zero. A car spawning between two polls, or the exit portal, are candidates and neither was shown.

tunnel-wall-test, tunnel-test and smoke pass on the span-by-span tube (EVD-755 to EVD-757).

## WHAT IS STILL VISIBLE IN THE CAPTURES AND IS NOT FIXED

- At the far end on a bend, a pale band with thin vertical lines juts sideways over the nearer wall. It is most likely the far rings' panel joints, which are stroked after the wall faces and are not clipped.
- A red wedge along the lower left wall on some bends.
- The lamps in the far darkness are not clipped either.
The owner has not been asked about these yet.
- **RULED 2026-09-13T16:09:21-04:00** — **THE SECOND REPAIR, v0.14.17. NOT SIGNED** ([[RLG-116]]). Owner, 2026-09-13, with a capture of my own winding tunnel: *"You can clearly see the road and the vehicles on it beyond the curve being drawn through the wall."*

## THE ORDER WAS WRONG, NOT ONLY THE FILL RULE

The first repair fixed the fold but painted the tube in PASSES with no depth: all vault, all joints, then each wall's faces, outsides and barrier strips, and then the held-back cars ([[RLG-145]]) over everything. On a bend the far barrier strip lay over the near wall face, far joints over it, and every car beyond the bend went on top.

- `drawBore` now prepares the frame and paints only the backdrop. `paintBoreSpan` paints one span: vault halves, both faces, outsides, the joints of rings standing in it, and the barrier. `paintBoreSpans` walks spans FAR TO NEAR and paints the held-back car buckets between them, so a nearer span covers everything behind it. `boreStrip` is deleted.
- A bucket is placed by its segment's FAR end, so a car just past a span boundary is painted before the nearer span.
- `spriteDefer` was armed on `placeDark() > 0.01` while `drawBore` asks `boreSpan()`; approaching a mouth the cars painted under an already visible tube. It now asks `boreSpan()`.
- `boreClipOut` (shared) cuts nearer wall spans out of the clip for the far darkness, the lamps (walked near to far), and the exit portal, all painted after the tube.

## THE PROOF, AND WHAT IS STILL OPEN

`API.boreOcclusion` now reads PIXELS: points across the carriageway at road and car height that a nearer wall span hides must read pale grey, not dark tarmac, black or a coloured car. A police car is parked on the next frame where the probe found hidden road, because random traffic is not always behind a wall (EVD-771 proved nothing that way).

| Evidence | Result |
|---|---|
| EVD-776, EVD-783 | fixed: 0 of 14,420 and 0 of 3,040 hidden points through; darkness 0 |
| EVD-780 | fixed, before cars were placed by the segment's far end: 3 of 3,863 through, 1 dark - FAILED |
| EVD-784 | `--falsify cars`: 306 of 5,480 through, only that check failed |
| EVD-782 | `--falsify dark`: road and darkness checks failed, as expected |
| EVD-777, EVD-778, EVD-781 | falsifier runs that tripped the precondition or had too little bend; the threshold was then changed. Left standing. |
| EVD-773, EVD-774 | tunnel-wall-test and tunnel-test pass |
| EVD-775, EVD-779 | smoke 5/6: Quietus's erase control timed out. quietus.html does not load road.js, so this unit cannot cause it. Not investigated. |

**UNEXPLAINED RESIDUE:** in 1 of 4 unrecorded runs after the last change, 4 warm pale pixels (about 181, 153, 134) read through at 32,189-32,389 units, at the tube's far end (reach 33,060), with lamps off as well. The source is not found. Six captures of a winding tunnel show no road or car through a wall. **The owner judges it on the device.**

The probe is slow, about three minutes a run.
