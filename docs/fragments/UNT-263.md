+++
id                = "UNT-263"
kind              = "unit"
status            = "closed"
cost              = 120300
closed            = "2026-09-10T22:19:28-04:00"
created           = "2026-09-10T21:28:38-04:00"
created_precision = "second"
source            = "the thread's stated next action"
supersedes        = []
blocks            = []
caused_by         = ["RLG-203"]
see_also          = []
delegate          = false
effort            = "medium"
model             = "claude-sonnet-5"
ran_on            = "claude-opus-5"
routing           = "feature"
+++

### UNT-263 - INTERCEPT is a mode a police car can enter, and the race modes are not

- Routing class: `feature` -> **claude-sonnet-5**, effort **medium**. Bias 1. Implementation to a spec that already answers the design questions. ([[RLG-092]])
- Opened for: [[RLG-203]]
- Opened by `unit.py`. **The identity exists before the first edit** ([[RLG-097]]), so the snapshot has somewhere to go.
- **CLOSED 2026-09-10T22:19:28-04:00** - INTERCEPT exists and can be entered; it cannot yet be played. A police car's garage offers TEST DRIVE and INTERCEPT and no race mode, INTERCEPT starts a race of the matching class with hot pursuit forced on, and the world has stopped coming after the player. Nothing scores, nothing is won and nothing is lost - that is RLG-203's next unit.

IT IS A RACE WITH A FLAG, NOT A THIRD VALUE OF `mode`. Seventeen places branch on `mode === 'race'` and every one of them is true of this mode, because the owner's own description is "identical except". A third value would have had to answer all seventeen and would have got one wrong.

THE FOUR PLACES THE WORLD COMES AFTER THE PLAYER are each shut at their own seam: `addHeat`, which is the one way in; `worth`, which is the whole of whether the player is a candidate for a cruiser's search; the patrol and the trap, which pull over a speeding car and on shift that car is the player on purpose; and the BUST, which reads neither `heat` nor `worth` and would have arrested a stopped police car for parking beside a colleague.

RACE_BANNED gained both police cars in the same unit, because a police car with TEST DRIVE and nothing else is a prize that unlocks less than the day before. `dutyLegal` reads `force` off the record rather than naming two bodies. This supersedes half of RLG-202: the class survives and still picks the field, the race ENTRY is gone, and interceptor-test now asserts both halves of the trade.

THE HARNESS FOUND TWO DEFECTS READING DID NOT. Walking CRUISER to SUPERCRUISER dropped the shift, because the route passes racing cars; and one shared `mode` made the two settings fight, so a player who set TOURNAMENT and went to look at the police cars came back to SINGLE RACE. A third was mine: the rule forcing HOT PURSUIT on sat after an early `return` and never ran, so the control read OFF while being shut.

Every road check runs twice in the SAME car, on shift and off, because a guard that is never exercised is not a guard. Three control arms were rebuilt after failing honestly, all for one reason: a cruiser tops out at 0.71 of the player's speed, so a harness holding 0.92 leaves every staged cruiser behind and culls it before the read.

NOT SIGNED OFF (RLG-116), and one guard is unproved: the speed trap, because there is no affordance to place one where a harness needs it. It shares its line with the patrol guard, which is proved.
