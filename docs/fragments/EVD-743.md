+++
id                = "EVD-743"
kind              = "evidence"
status            = "recorded"
step              = "with the bore limit removed, exactly the camera and car-side checks fail"
unit              = "UNT-300"
command           = "P:\\Code_Continuum\\Projects\\In-Dev\\Effigy_Arcade\\.venv\\Scripts\\python.exe tools/falsify-expect.py the camera never leaves the bore|the car never passes through the wall -- P:\\Code_Continuum\\Projects\\In-Dev\\Effigy_Arcade\\.venv\\Scripts\\python.exe tools/tunnel-wall-test.py --falsify"
exit_code         = 1
duration_ms       = 20260
output_bytes      = 938
output_sha        = "5ae0a10c8f745f415535956ad8ccf252"
store_stamp       = "ce9a85b7dfc19171"
created           = "2026-09-13T13:14:26-04:00"
created_precision = "second"
supersedes        = []
blocks            = []
caused_by         = []
see_also          = []
+++

### EVD-743 — step `with the bore limit removed, exactly the camera and car-side checks fail` of [[UNT-300]]

- Ran: `P:\Code_Continuum\Projects\In-Dev\Effigy_Arcade\.venv\Scripts\python.exe tools/falsify-expect.py the camera never leaves the bore|the car never passes through the wall -- P:\Code_Continuum\Projects\In-Dev\Effigy_Arcade\.venv\Scripts\python.exe tools/tunnel-wall-test.py --falsify`
- Result: **EXIT 1 — the step FAILED**, in 20260 ms
- Output: 938 bytes, sha256 `5ae0a10c8f745f415535956ad8ccf252`
- Store stamp at the time: `ce9a85b7dfc19171`

**Output** — head only; the digest above covers the whole of it, so this excerpt cannot be selective without saying so.

```
tunnel-wall-test  .  the car and the camera stay inside the bore
  FALSIFY: the bore limit is removed. Checks 2 and 3 must fail.
  browser: installed chrome - no bundled chromium, so the engine version is whatever this machine has
  ok    on the open road the car still reaches the old edge   furthest out 1.150
  ok    right the car is held hard over inside the bore   furthest out 1.150, 40 samples
  ok    right the camera never leaves the bore   closest to the wall 118.8 px
  FAIL  right the car never passes through the wall   closest to the wall -9.8 px
  ok    left  the car is held hard over inside the bore   furthest out 1.150, 40 samples
  ok    left  the camera never leaves the bore   closest to the wall 118.8 px
  FAIL  left  the car never passes through the wall   closest to the wall -9.8 px
  2 check(s) FAILED
falsify-expect: WRONG. unexpected failures [], expected but not failed ['the camera never leaves the bore']
```

*Written by `step.py` as a byproduct of running the step. **Nobody attested to this** ([[RLG-081]]).*
