+++
id                = "RVW-001"
kind              = "review"
status            = "recorded"
unit              = "UNT-412"
created           = "2026-09-21T23:27:44-04:00"
created_precision = "second"
source            = "review of UNT-412, scoped to `review.py --packet`"
supersedes        = []
blocks            = []
caused_by         = []
see_also          = ["UNT-412"]
files             = ["arcade.js", "road.js", "tools/glass-survey-test.py"]
findings          = 5
packet            = "d86183fa3daefd29"
reviewed_lines    = 646
verdict           = "sound-with-findings"
+++

### RVW-001 — review of [[UNT-412]] — sound-with-findings

- **Scope**: 3 file(s), 646 code line(s). The `files` field is the diff at the moment this was filed, read from git rather than typed by the reviewer.
- **Packet**: `d86183fa3daefd29`. The digest of the packet the reviewer was handed was recomputed when this was filed and matched, so the diff recorded above is the diff that was read.
- **Findings**: 5.

Fresh-eyes review of UNT-412 by a separate agent. Verdict: sound with findings. The windscreen draws exactly what it drew before; no canvas state leak, no out-of-scope variable, no wrong geometry among the lamp loop, joint loop, portal boundary and lookIn direction.

1. MEDIUM - the survey could pass without exercising the five new layers: `ask - g` only asks what the windscreen painted, so a tour in which the windscreen never paints range, lamp, joint or portal passes empty, and --falsify did not assert which checks failed. FIXED in this unit: the survey now asserts each of the five appears out of the windscreen during the tour.
2. LOW - the glass range's lane term had the wrong sign against rproj's -camX, so on a lane change it slid against the drop it stands on. FIXED.
3. LOW - the glass kerb stripes on the 900-unit step, not RUMBLE's 600, and mixed snow at 0.55 against the windscreen's 0.45. Snow mix FIXED to 0.45; the stripe pitch is left, because the glass's lane dashes use the same step and a 44-pixel pane cannot show the difference.
4. LOW - two minimum widths in paintStreetLamp were not scaled by k, so a far post in the glass got a 0.6 px highlight and a 1 px arm. FIXED; k = 1 leaves the windscreen unchanged.
5. LOW - a portal behind you vanishes if the next crossing starts within about 170 segments, because a crossing in flight takes biomeEdge. Left: bioBehind drops growLeft during a crossing too, so the whole glass behaves this way, and it is a rare case.
