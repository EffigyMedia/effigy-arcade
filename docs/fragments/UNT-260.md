+++
id                = "UNT-260"
kind              = "unit"
status            = "closed"
cost              = 50510
closed            = "2026-09-10T18:00:50-04:00"
created           = "2026-09-10T17:41:12-04:00"
created_precision = "second"
source            = "owner, 2026-09-10"
supersedes        = []
blocks            = []
caused_by         = ["RLG-207"]
see_also          = []
delegate          = false
effort            = "medium"
model             = "claude-sonnet-5"
ran_on            = "claude-opus-5"
routing           = "feature"
+++

### UNT-260 - A merge is decided like a person

- Routing class: `feature` -> **claude-sonnet-5**, effort **medium**. Bias 1. Implementation to a spec that already answers the design questions. ([[RLG-092]])
- Opened for: [[RLG-207]]
- Opened by `unit.py`. **The identity exists before the first edit** ([[RLG-097]]), so the snapshot has somewhere to go.
- **CLOSED 2026-09-10T18:00:50-04:00** - RLG-207's four deltas are built and NOT PROVEN. The indicator habit is assigned at spawn from the personality (0.95 commuter, 0.75 speeder, 0.25 outlaw, 0 racer), the announce wait is a named 1-3 second tunable, the car re-reads its surroundings every quarter second while announcing, and it can cancel - lamp off, mergeCool set by lookAgain so an outlaw comes back to the idea sooner, counted as mergesAborted apart from signalledMerges because an announcement that never completes is also what a defect looks like. Corrected the record first: the mirror check EXISTS in laneClear and this ruling said it did not - four deltas, not five. Two faults caught while writing: the cancel was first a  which would have skipped the rest of the car's frame, and mergeLook is guarded against NaN because that failure would be silent. NOT PUSHED: tools/merge-mind-test.py has never produced a result. This machine stopped running browser harnesses today - smoke fell to 4/6 on both driving cabinets, and it fails IDENTICALLY with road.js stashed at the last commit. A probe shows window.__road present, canvas present, zero page errors and readyState stuck at interactive: the engine boots and the document never completes, so load never fires. Second occurrence; a reboot cleared it last time. Filed RLG-208 - 110 harness call sites wait on load when what they need is the engine.
- **RULED 2026-09-10T18:01:06-04:00** — One word was lost from the why above: the cancel was first written as a JavaScript **continue** statement, which would have skipped the rest of that car's frame. It was elided because the shell that ran this close read the backticks around it as a command substitution.
