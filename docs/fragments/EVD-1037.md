+++
id                = "EVD-1037"
kind              = "evidence"
status            = "recorded"
step              = "no rival snaps to a new speed at GO (RLG-276)"
unit              = "UNT-366"
command           = "P:\\Code_Continuum\\Projects\\In-Dev\\Effigy_Arcade\\.venv\\Scripts\\python.exe tools/launch-test.py"
exit_code         = 0
duration_ms       = 49821
output_bytes      = 2834
output_sha        = "79b3efa93778dbfe86a1d11ec82c7c22"
store_stamp       = "9bc18e57fe878ccd"
created           = "2026-09-18T13:00:23-04:00"
created_precision = "second"
supersedes        = []
blocks            = []
caused_by         = []
see_also          = []
+++

### EVD-1037 — step `no rival snaps to a new speed at GO (RLG-276)` of [[UNT-366]]

- Ran: `P:\Code_Continuum\Projects\In-Dev\Effigy_Arcade\.venv\Scripts\python.exe tools/launch-test.py`
- Result: **exit 0**, in 49821 ms
- Output: 2834 bytes, sha256 `79b3efa93778dbfe86a1d11ec82c7c22`
- Store stamp at the time: `9bc18e57fe878ccd`

**Output** — head only; the digest above covers the whole of it, so this excerpt cannot be selective without saying so.

```
launch-test  .  the grid starts from rest and the player launches from neutral
  browser: installed chrome - no bundled chromium, so the engine version is whatever this machine has

  the box, and the engine in it
  ok    the count runs long enough to launch in
  ok    the box is in neutral for the whole count, on the automatic gearbox
  ok    and the launch is armed while it is held
      needle with the pedal flat: 0.988 of the redline at its highest
  ok    a held throttle winds the engine to the limiter
      walking the needle: 0.091 to 0.801, largest single drop 0.018
  ok    and the needle falls again when the throttle is released
  ok    the engine voice exists while the count is up
      engine pitch across the count: 70.2 Hz to 454.6 Hz
  ok    and its pitch climbs with the revs rather than sitting at idle
  ok    on the context the game is playing through, not an orphaned one
  ok    and it is audible rather than held at zero
  ok    and the highest pitch belongs to the highest revs
  ok    and it idles when the throttle is never touched

  the window, and the three things that can happen at it
      none   needle 0.115   band 0.612-0.876   BOGGED
      flat   needle 0.966   band 0.612-0.876   WHEELSPIN
      aim    needle 0.746   band 0.612-0.876   PERFECT LAUNCH
  ok    the none start landed under the band and the game said so
  ok    the flat start landed over the band and the game said so
  ok    the aim start landed inside the band and the game said so
      speed a second after GO:  none 249   flat 42   aim 1540
  ok    at least one of the three starts landed inside the window
  ok    a launch inside the window beats the none start
  ok    a launch inside the window beats the flat start

  the rolling start, on the endless road
      the machine rolls to the line at 3833 units (50 mph), standing=False
  ok    Interstate does not start from a grid
  ok    and it has a rolling speed to start at
  ok    the car is held at the rolling speed early in the count
  ok    the car is held at the rolling speed late in the count
      the road passed under it by 4600 units while the count ran
  ok    and the road is passing under it while the count runs
  ok    a race grid is eleven rivals
... 11 more line(s), covered by the digest
```

*Written by `step.py` as a byproduct of running the step. **Nobody attested to this** ([[RLG-081]]).*
