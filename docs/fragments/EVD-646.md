+++
id                = "EVD-646"
kind              = "evidence"
status            = "recorded"
step              = "how far one timed test drive reaches before the clock ends it, with a falsifier that proves the harness can report a long run"
unit              = "UNT-268"
command           = "P:\\Code_Continuum\\Projects\\In-Dev\\Effigy_Arcade\\.venv\\Scripts\\python.exe tools/reach-test.py"
exit_code         = 1
duration_ms       = 761917
output_bytes      = 1209
output_sha        = "35131fb4e7c0c037c3d7ab6ce9f05681"
store_stamp       = "4f1ea2fa753e25a6"
created           = "2026-09-12T03:42:13-04:00"
created_precision = "second"
supersedes        = []
blocks            = []
caused_by         = []
see_also          = []
+++

### EVD-646 — step `how far one timed test drive reaches before the clock ends it, with a falsifier that proves the harness can report a long run` of [[UNT-268]]

- Ran: `P:\Code_Continuum\Projects\In-Dev\Effigy_Arcade\.venv\Scripts\python.exe tools/reach-test.py`
- Result: **EXIT 1 — the step FAILED**, in 761917 ms
- Output: 1209 bytes, sha256 `35131fb4e7c0c037c3d7ab6ce9f05681`
- Store stamp at the time: `4f1ea2fa753e25a6`

**Output** — head only; the digest above covers the whole of it, so this excerpt cannot be selective without saying so.

```
reach  .  how far ONE timed test drive gets before the clock ends it
  browser: installed chrome - no bundled chromium, so the engine version is whatever this machine has

  FALSIFIER - can this harness report a LONG run at all?
      TUNER with the speed pinned at 1000mph, timer ON
        10.45 miles in 60s of wall clock, clock 60.0 -> 112.6 (lowest 49.9)
  FAIL  the harness can report a run past 100 miles, so a short answer below is the game

  HOW FAR DOES A REAL CAR GET, FLAT OUT, TIMER ON
      checkpoints are 2 miles apart and pay 20s, a crate pays 10s,
      and the run starts with 60s on the clock
      SALOON       0.00 miles   ABANDONED at the wall-clock cap after 240s   0 crate(s)
                 peak 0 mph, lowest clock 60.0
      TUNER        3.77 miles   the clock ran out after 112s   3 crate(s)
                 peak 115 mph, lowest clock 0.0
      MUSCLE       3.14 miles   the clock ran out after 82s   0 crate(s)
                 peak 115 mph, lowest clock 0.0
      STALLION     0.00 miles   ABANDONED at the wall-clock cap after 240s   0 crate(s)
                 peak 0 mph, lowest clock 60.0

FAILED: the falsifier did not clear 100 miles - every number below is unreadable
```

*Written by `step.py` as a byproduct of running the step. **Nobody attested to this** ([[RLG-081]]).*
