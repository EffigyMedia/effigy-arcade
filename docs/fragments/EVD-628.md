+++
id                = "EVD-628"
kind              = "evidence"
status            = "recorded"
step              = "boot() brings a cabinet up through a document that never finishes loading, and still returns False when no engine arrives"
unit              = "UNT-262"
command           = "P:\\Code_Continuum\\Projects\\In-Dev\\Effigy_Arcade\\.venv\\Scripts\\python.exe tools/boot-proof.py"
exit_code         = 0
duration_ms       = 25411
output_bytes      = 1129
output_sha        = "838fcdc7d0e0ced4dbf1eb09c944f904"
store_stamp       = "1c1387f0f5b0e7f1"
created           = "2026-09-10T19:46:12-04:00"
created_precision = "second"
supersedes        = []
blocks            = []
caused_by         = []
see_also          = []
+++

### EVD-628 — step `boot() brings a cabinet up through a document that never finishes loading, and still returns False when no engine arrives` of [[UNT-262]]

- Ran: `P:\Code_Continuum\Projects\In-Dev\Effigy_Arcade\.venv\Scripts\python.exe tools/boot-proof.py`
- Result: **exit 0**, in 25411 ms
- Output: 1129 bytes, sha256 `838fcdc7d0e0ced4dbf1eb09c944f904`
- Store stamp at the time: `1c1387f0f5b0e7f1`

**Output** — head only; the digest above covers the whole of it, so this excerpt cannot be selective without saying so.

```
boot-proof  .  the harness waits for the engine, not for the document
  browser: installed chrome - no bundled chromium, so the engine version is whatever this machine has
  PASS  control: the launcher fires `load` with nothing held   0.31s
  PASS  a held subresource stops `load` firing   never
  PASS  and the page is stuck at readyState `interactive`   interactive
  PASS  while the page itself raised no error   0 error(s)
  PASS  boot() brings the launcher up through the same wedge   0.19s
  PASS  and the shell is genuinely attached, not merely present
  PASS  the document is still unfinished when boot() returns   interactive
  PASS  boot() brings a DRIVING cabinet up through the wedge   0.86s
  PASS  and the engine is RUNNING, not merely constructed   phase 0.0002 -> 0.0004
  PASS  and it has painted a real frame   canvas 45154
  PASS  boot() returns False for a page with no engine at all   5.55s
  PASS  boot(ready='road') returns False on the launcher, which has no driving engine
  PASS  though the launcher was reachable - the failure is the ENGINE, not the page
  PASS  the URL picks the engine

0 failure(s)
```

*Written by `step.py` as a byproduct of running the step. **Nobody attested to this** ([[RLG-081]]).*
