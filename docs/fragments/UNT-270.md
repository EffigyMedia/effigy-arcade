+++
id                = "UNT-270"
kind              = "unit"
status            = "closed"
cost              = 32371
closed            = "2026-09-12T09:14:16-04:00"
created           = "2026-09-12T09:00:07-04:00"
created_precision = "second"
supersedes        = []
blocks            = []
caused_by         = ["RLG-171"]
see_also          = []
delegate          = false
effort            = "medium"
model             = "claude-sonnet-5"
ran_on            = "claude-opus-5"
routing           = "feature"
+++

### UNT-270 - A cruiser takes traffic damage wherever it is, like every other car on the road

- Routing class: `feature` -> **claude-sonnet-5**, effort **medium**. Bias 1. Implementation to a spec that already answers the design questions. ([[RLG-092]])
- Opened for: [[RLG-171]]
- Opened by `unit.py`. **The identity exists before the first edit** ([[RLG-097]]), so the snapshot has somewhere to go.
- **CLOSED 2026-09-12T09:14:15-04:00** - RLG-171 built, v0.13.85. The cruiser-versus-traffic collision ran only under k.z > pz - 900, so a cruiser more than 900 units behind the player was tested against nothing - and the police only ever arrive from behind. The guard is removed rather than widened, because it was the only positional exemption from traffic damage on the road: a rival racer is tested with no reference to the player at all, and so is the player's own car. tools/coptraffic-test.py is the proof and its first version was wrong - it counted KILLS and read zero on both builds, because a cruiser needs several hits and the last one lands after it has closed back on the player. Counting HITS separates them: six in the band with the guard out, zero with it back, on runs that spent 11,491 and 10,779 cruiser-frames in that band. The check is BLKD rather than passed when no cruiser was ever in the band. THE PACKER PASSES and its step evidence does not say so - node cannot open stdout under the step runner's capture and fails with EISDIR before pack.sh runs, so EVD-656, EVD-657 and EVD-658 are artifacts of the recorder. Run directly, pack.sh --check reports all checks pass at cache v52. ROAD_BUILD had drifted fourteen versions behind Arcade.version, which makes buildTag() report MIXED on every device; both are bumped here and a task is queued to stop it recurring. WHAT NOBODY HAS JUDGED: the owner said the police were constantly destroying themselves and six hits in ninety seconds is not constant. hurtCop's 45 is the single tunable.
