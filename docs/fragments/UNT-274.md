+++
id                = "UNT-274"
kind              = "unit"
status            = "closed"
cost              = 33120
closed            = "2026-09-12T11:15:48-04:00"
created           = "2026-09-12T10:55:34-04:00"
created_precision = "second"
supersedes        = []
blocks            = []
caused_by         = ["RLG-213"]
see_also          = []
delegate          = false
effort            = "medium"
model             = "claude-sonnet-5"
ran_on            = "claude-opus-5"
routing           = "feature"
+++

### UNT-274 - The ladder: production unlocks sports, sports unlocks super, and a fresh save starts with production only

- Routing class: `feature` -> **claude-sonnet-5**, effort **medium**. Bias 1. Implementation to a spec that already answers the design questions. ([[RLG-092]])
- Opened for: [[RLG-213]]
- Opened by `unit.py`. **The identity exists before the first edit** ([[RLG-097]]), so the snapshot has somewhere to go.
- **CLOSED 2026-09-12T11:15:47-04:00** - RLG-213's ladder, v0.13.89. A production gold unlocks sports, so the rungs run production, sports, super, formula; classOf gained a production league and production came out of RACE_BANNED in the same change, which was the condition set when the resort landed. A fresh save holds production only - the three sports cars gain BODY_CLASS rows, which retires that table's oldest convention that a missing row meant open-from-the-start, and the default car is a SALOON. The ladder is one table because the finish, the caption and the harness all ask it: a probe was first written as a COPY of the four-way branch and deleted inside a minute. A production car carries no bottle by ruling now rather than by accident - it was never a candidate when hasNosFor was written, and the obvious tidy-up once production is a league is to add it for symmetry. drive-test caught the change twice and was right both times: its expected-cars list follows the bottom of the ladder, and five bottle checks were describing the harness's own SALOON, so it seeds a won production tournament and drives a TUNER - 36 of 37 now, the failure being the recorded environmental flake at 100.5%. AND THE CHECK USED THE WRONG INSTRUMENT TWICE IN OPPOSITE DIRECTIONS: the card name passed with the migration deleted because a secret class is absent when locked, and garage membership then passed with the sports cars wide open because a non-secret locked class is listed as a silhouette. Only playableBodies answers ownership, and only putting each defect back found either.
