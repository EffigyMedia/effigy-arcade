+++
id                = "EVD-1018"
kind              = "evidence"
status            = "recorded"
step              = "the cliff rim is jagged, uneven, fixed to the road, and drawn in both views"
unit              = "UNT-364"
command           = "P:\\Code_Continuum\\Projects\\In-Dev\\Effigy_Arcade\\.venv\\Scripts\\python.exe tools/cliff-test.py"
exit_code         = 0
duration_ms       = 17916
output_bytes      = 1554
output_sha        = "7bfbaea2c5a49fe91a9badf7db034899"
store_stamp       = "c7eb2eb00635d989"
created           = "2026-09-17T07:54:37-04:00"
created_precision = "second"
supersedes        = []
blocks            = []
caused_by         = []
see_also          = []
+++

### EVD-1018 — step `the cliff rim is jagged, uneven, fixed to the road, and drawn in both views` of [[UNT-364]]

- Ran: `P:\Code_Continuum\Projects\In-Dev\Effigy_Arcade\.venv\Scripts\python.exe tools/cliff-test.py`
- Result: **exit 0**, in 17916 ms
- Output: 1554 bytes, sha256 `7bfbaea2c5a49fe91a9badf7db034899`
- Store stamp at the time: `c7eb2eb00635d989`

**Output** — head only; the digest above covers the whole of it, so this excerpt cannot be selective without saying so.

```
cliff-test  .  the cliff edge is broken rock

  browser: installed chrome - no bundled chromium, so the engine version is whatever this machine has
  THE RIM, SEGMENT BY SEGMENT
      tunables {'amp': 0.16, 'bite': 0.42, 'odds': 0.3, 'run': 6}
      offset beyond the rail: min 0.000  max 0.535  mean 0.135  sd 0.118
  ok    the rim never comes inside the rail   min 0.0002
  ok    and it is not one even line   sd 0.1177
  ok    and neighbouring segments differ   532 distinct of 600
  ok    and it is NON-UNIFORM: some stretches bitten deep, some only rough   42 deep and 56 rough of 100 stretches
  ok    and it is the same when asked twice, so it does not shimmer

  AND THE RENDER USES IT
      cliff on the left  windscreen   382 px at x= 61.3   mirror   35 px
  ok    with the cliff on the left, the jag changes the windscreen   382 pixels
  ok    and the change is on the left, where the drop is   mean x 61.3 of 480
  ok    and the mirror carries the jag too   35 pixels against a bar of 18
      cliff on the right windscreen   624 px at x=381.4   mirror   40 px
  ok    with the cliff on the right, the jag changes the windscreen   624 pixels
  ok    and the change is on the right, where the drop is   mean x 381.4 of 480
  ok    and the mirror carries the jag too   40 pixels against a bar of 18
  ok    and two straight renders do not differ, so the diff is the jag   0 pixels
  ok    no page errors

  the rim is jagged, uneven, fixed to the road, and drawn in both views
  whether it reads as rock at speed is the owner call on a device.
```

*Written by `step.py` as a byproduct of running the step. **Nobody attested to this** ([[RLG-081]]).*
