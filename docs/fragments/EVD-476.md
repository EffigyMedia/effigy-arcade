+++
id                = "EVD-476"
kind              = "evidence"
status            = "recorded"
step              = "BASELINE: heat-test on the pre-retune engine, to tell my failures from the harness's own"
unit              = "UNT-200"
command           = "P:/Code_Continuum/Projects/In-Dev/Effigy_Arcade/.venv/Scripts/python.exe tools/heat-test.py"
exit_code         = 1
duration_ms       = 123734
output_bytes      = 1042
output_sha        = "a7356551736d103370e0e4ecf6bd11d2"
store_stamp       = "f3284a4ec87403d1"
created           = "2026-09-08T01:40:06-04:00"
created_precision = "second"
supersedes        = []
blocks            = []
caused_by         = []
see_also          = []
+++

### EVD-476 — step `BASELINE: heat-test on the pre-retune engine, to tell my failures from the harness's own` of [[UNT-200]]

- Ran: `P:/Code_Continuum/Projects/In-Dev/Effigy_Arcade/.venv/Scripts/python.exe tools/heat-test.py`
- Result: **EXIT 1 — the step FAILED**, in 123734 ms
- Output: 1042 bytes, sha256 `a7356551736d103370e0e4ecf6bd11d2`
- Store stamp at the time: `f3284a4ec87403d1`

**Output** — head only; the digest above covers the whole of it, so this excerpt cannot be selective without saying so.

```
heat-test  .  the wanted level is earned, not counted
  browser: playwright bundled chromium (pinned)
  ok    the pursuit system is running
      heat 2 -> 1 over 11s stopped on an empty road (cooling at 10.5 of 12)
  ok    time alone never raises the wanted level
      heat 4 -> 3 after 14s clear of every cruiser
  ok    outrunning them cools the wanted level
      a cruiser held 20,000 behind: the cool clock reached 40.9 of the 12 needed
  ok    a cruiser you have left behind stops holding the wanted level up
  ok    and it falls all the way to empty, which it could not before
      heat 5 at 160mph, no trap earned: 0 supers
  ok    no super cruiser without the 170 past a trap
      with the trap earned: 0 supers
  FAIL  and one is dispatched once it has been   [{'heat': 5, 'pts': 500, 'cool': 60.01, 'earned': True, 'chasing': 0, 'supers': 0, 'coolNeeds': 12, 'easy': False}]
      heat held at 2 with the trap earned: 0 supers, heat now 2  (printed, not asserted - see the note above)
  ok    no page errors

1 check(s) failed
```

*Written by `step.py` as a byproduct of running the step. **Nobody attested to this** ([[RLG-081]]).*
