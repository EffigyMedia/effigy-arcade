+++
id                = "EVD-613"
kind              = "evidence"
status            = "recorded"
step              = "the car sat on the 150 gate when the check asked for 160 and banked under the 4 seconds an interceptor needs; held every frame it reads a flat 160, and held BELOW the gate it still sends nothing"
unit              = "UNT-244"
command           = "P:/Code_Continuum/Projects/In-Dev/Effigy_Arcade/.venv/Scripts/python.exe tools/super-gate-proof.py"
exit_code         = 1
duration_ms       = 66093
output_bytes      = 1192
output_sha        = "5e2d93c96b1eb4a9935a96ec4d1e576b"
store_stamp       = "8da81cc52f125585"
created           = "2026-09-09T22:01:01-04:00"
created_precision = "second"
supersedes        = []
blocks            = []
caused_by         = []
see_also          = []
+++

### EVD-613 — step `the car sat on the 150 gate when the check asked for 160 and banked under the 4 seconds an interceptor needs; held every frame it reads a flat 160, and held BELOW the gate it still sends nothing` of [[UNT-244]]

- Ran: `P:/Code_Continuum/Projects/In-Dev/Effigy_Arcade/.venv/Scripts/python.exe tools/super-gate-proof.py`
- Result: **EXIT 1 — the step FAILED**, in 66093 ms
- Output: 1192 bytes, sha256 `5e2d93c96b1eb4a9935a96ec4d1e576b`
- Store stamp at the time: `8da81cc52f125585`

**Output** — head only; the digest above covers the whole of it, so this excerpt cannot be selective without saying so.

```
super-gate-proof  .  the check read zero because the car was not going fast enough
  browser: playwright bundled chromium (pinned)
      the rule under test: above 150mph for 4 unbroken seconds, at heat 3 or more
      OLD  asked for 160mph: the car read 151-158mph, under the gate on 0 of 64 samples; the longest unbroken stretch banked 11.35s of the 4s needed; 4 supers
  ok    a speed asked for four times a second is never actually reached
  FAIL  and it spends part of every second UNDER the gate the rule watches   [it never dropped below 151mph, so this is not the fault that was found]
      NEW  asked for 160mph: the car read 160-160mph, under the gate on 0 of 64 samples; the longest unbroken stretch banked 10.35s of the 4s needed; 4 supers
  ok    holding it keeps the car above the gate on every sample
  ok    so the unbroken seconds the rule asks for are actually banked
  ok    and an interceptor is dispatched
      SLOW asked for 140mph: the car read 136-140mph, under the gate on 64 of 64 samples; the longest unbroken stretch banked 0.00s of the 4s needed; 0 supers
  ok    below the gate, held just as steadily, nothing is sent
  ok    no page errors

1 check(s) failed
```

*Written by `step.py` as a byproduct of running the step. **Nobody attested to this** ([[RLG-081]]).*
