+++
id                = "RLG-037"
kind              = "ruling"
type              = "defect"
status            = "requested"
created           = "2026-08-28T16:06:06-04:00"
created_precision = "second"
supersedes        = []
blocks            = []
caused_by         = []
see_also          = []
+++

### RLG-037 — Interstate: scatter and the lane-open guarantee pull against each other
- **RULED 2026-08-28T16:06:42-04:00** — - **The two systems pull against each other.** `scatter` moves a car sideways because it was asked to;
  `keepLaneOpen` guarantees a car-width corridor always exists. A scattered car can be the one that
  closes the road, and `keepLaneOpen` only corrects it on a later frame.

- **Measured across six runs of `traffic-test` with the horn phase active**, the narrowest corridor
  came out at **0.227, 0.323, 0.363, 0.418, 0.428 and 0.452** against a 0.34 limit. **Two of the six
  are under it.** Without the horn phase the same test sat comfortably at 0.418 to 0.452.

- **So the guarantee is not absolute while the horn is in use**, and the test is stochastic - it will
  pass most runs and fail some. That is worth more than a green tick: a check that passes four times
  in six is a check that will be believed and should not be.

- **One fix was tried and made it worse.** Judging a merge on its swept path - treating the car as
  occupying everything between where it is and where it is going, which is what it physically does -
  took the corridor from 0.323 to **0.227**. It was reverted. The reason it backfired is not
  understood, and guessing again at the end of an unrelated unit is how the occlusion code ended up
  disabled for a release.

- **Where to start.** `keepLaneOpen` runs once per frame before the traffic loop, and `scatter` runs
  from input handling, so a scattered car blocks for at least one frame before anything looks at it.
  The ordering is the first thing to check. The second is whether the corridor test should consider
  a car's committed merge target rather than only its current position - the opposite of the swept
  path, and untried.
- **RULED 2026-08-28T16:22:09-04:00** — - **IMPROVED, MEASURABLY, BUT NOT CLOSED.** `wouldBlock` gathered cars within +/-1600 of the mover -
  one window's width, centred on the car. But `keepLaneOpen` measures windows 1600 wide **stepped
  every 800**, so a car near a window boundary could pass its own centred check and still close the
  window beside it - which is the one that reports the road blocked. Reaching a full window either
  side (+/-2400) means every window that will later judge the car is inside what is checked now.

- **The measurement series, which is the honest record of where this stands.**

  | Condition | Narrowest corridor (limit 0.34) |
  |---|---|
  | before any fix | 0.227, 0.323 (both failing), 0.363, 0.418, 0.428, 0.452 |
  | after the +/-2400 reach | 0.463, 0.378, 0.342, **0.300** |

  So it fails roughly one run in four instead of one in three, and the passing runs sit higher. **That
  is an improvement and it is not a guarantee**, and the difference matters because the whole point of
  this rule is that it is absolute.

- **A longer horn phase reproduces it far more reliably than driving does**, which is the most useful
  thing learned here: scatter is the pressure, not ordinary merging. That is the condition to develop
  against.

- **One approach tried and rejected with evidence.** Judging a merge on its swept path - treating the
  car as occupying everything between where it is and where it is going, which is what it physically
  does - made it **worse**, 0.323 to 0.227. Reverted. Why it backfired is still not understood, and
  that is worth knowing before it is tried again.

- **Untried, and the next thing to look at.** `keepLaneOpen` runs once per frame before the traffic
  loop while `scatter` runs from input handling, so a scattered car blocks for at least one frame
  before anything examines it. Ordering is the first suspect. The second is whether the corridor test
  should consider a car's committed merge TARGET rather than its current position - the opposite of
  the swept path.
- **RULED 2026-08-29T04:21:45-04:00** — **MEASURED 2026-08-29, no change made, and it clears [[RLG-040]] of a suspicion I had put on it.**

`traffic-test` failed the corridor check twice in a row after v0.9.16 - 0.213 and 0.313 against the
0.34 limit - and 0.213 was below anything this fragment had previously recorded. The obvious suspect
was RLG-040's change to giving way: before it, a car that yielded stayed leaning on the verge for
the rest of its life, which left a permanent hole in the traffic; now the yield ends and the car
rejoins its lane. That reads exactly like a corridor being handed back.

**A/B, three runs each arm, seeded so both meet the same grid.** The control patches ONE line in the
server, in memory: the yield timer is set so it never expires, which is precisely the old behaviour.
Everything else is identical.

| | tightest corridor, three runs | samples with a blocked window |
|---|---|---|
| yield ends (as it ships) | 0.300, 0.473, 0.382 | 4, 0, 0 |
| yield never ends (pre-RLG-040) | **0.247, 0.208, 0.403** | 3, 4, 0 |

**The yield ending is not the cause, and freezing it is WORSE.** That is the opposite of what I
expected and it makes sense once seen: a car parked on the verge at 48mph forever is not a corridor,
it is an obstacle, and the rest of the traffic stacks up behind it.

**What the measurement actually says is that this fragment's own defect is bigger than recorded
here.** The guarantee is described as failing about one run in four. In six runs across two arms it
went under the limit in five, and produced a fully blocked window in three. **It is closer to a
coin toss than to one in four**, and both arms fail, so it is not a regression from anything built
this week.

**Nothing was tuned toward it.** The temptation with a number like 0.208 is to widen a margin until
the check goes green, and the fragment's own history says why that is wrong: the merge logic and
this guarantee pull against each other, and the last time one was tuned to satisfy the other the
result was weaving. This entry exists to correct the failure rate on the record and to remove a
suspect, not to fix it.
- **RULED 2026-09-09T20:45:09-04:00** — **MEASURED OVER TWENTY-FOUR RUNS, 2026-09-09, and the answer is that the guarantee is the defect
rather than the threshold.** The owner asked which of two things was true: that the road genuinely
closes now and then, or that a 0.34 limit is drawn too fine for a stochastic road. It is the first,
and by a wider margin than this fragment has recorded before.

**The instrument.** `tools/corridor-falsify.py` is committed; the survey that produced the table was
a scratch script, and the two numbers it needed are now permanent engine readings - `API.tightestAt`
and `API.blockedNearest`, which say how far ahead of the CAR the narrowest window and the nearest
closed window sat. Twelve page loads an arm, forty-five seconds each at a held 11,000 units a second,
one hundred and eighty samples a run. The control patches one line in the server, in memory, so
`keepLaneOpen` still MEASURES and never CORRECTS; it was proved live by counting cars carrying
`yielding`, which came out zero on every control run and four to six on every treatment run.

| | narrowest corridor per run, min / median / max | runs under 0.34 | blocked samples |
|---|---|---|---|
| the guarantee running, as it ships | 0.194 / 0.370 / 0.457 | **3 of 12** | 7 of 2,160 |
| its correction switched off | 0.175 / 0.500 / 0.556 | **4 of 12** | 8 of 2,160 |

**THE THRESHOLD IS NOT THE PROBLEM, and the shape of the numbers is how you can tell.** The failures
are 0.175, 0.185, 0.194, 0.205, 0.213 and 0.259. The passes are 0.34 to 0.56. There is a gap between
the two groups with nothing in it, which is an EVENT and not the tail of a distribution. A limit
drawn too fine would show a continuous spread straddling 0.34, and the owner's own 0.332 - the near
miss that prompted the question - turns out to be the rarer case rather than the typical one.

**AND THE CORRECTION IS NOT MEASURABLY WORTH RUNNING.** Four runs in twelve close with it off and
three in twelve with it on, which is not a difference twelve runs can resolve; and the MEDIAN
corridor is WIDER without it, 0.500 against 0.370. That is the opposite of what this fragment has
assumed since it was opened. The mechanism it suggests is the one already recorded here for the
frozen yield: `keepLaneOpen` slows the car furthest up the window and drags it toward the verge, and
a slowed car is an obstacle the rest of the field stacks up behind. The fixer may be building the
walls it then opens.

**WHERE THE ROAD CLOSES, which is new and is what makes a failure legible.** Across the seven blocked
samples in the treatment arm the nearest closure was 3,200 units ahead and the median 8,000. At the
pace driven that is 0.29 and 0.73 seconds. So these are not transients twenty thousand units up the
road that the fixer has time to open - they are in front of the car.

**AND THEY ARE REAL GEOMETRY, not an artefact of the window.** Three blocked windows were photographed
in full. Each holds three or four cars spread across all four lanes over two to three car lengths of
road, at different speeds, with the widest clear corridor between them at 0.283 lane units against a
car 0.26 wide. Two of the four cars in the tightest one were carrying `yielding` - they had been
told to give way and were part of the wall anyway.

**ONE WRONG READING WAS MADE AND CORRECTED, and it is worth keeping.** The first photographs
disagreed with the engine by a whole lane - the engine said 0.205 where the dump said 0.663 - because
`keepLaneOpen` is called with `pos + PLAYER_Z` and the dump was keyed to `pos`. That is about 880
units, which is over half the window, so the two were reading different stretches of road. It is the
same offset that made the horn look inert years ago. **Anything that reads the traffic array against
this guarantee must measure from the car and not from the camera.**

**WHAT IS STILL OPEN, and it is now the whole of it.** The corridor guarantee does not hold, it holds
no better than not running at all, and the first thing to test is whether its own slowing is the
cause - the `worst.cruise` reduction and the lean are two separate levers and only one of them has
ever been suspected. That is its own unit and it is not this one; this unit changed the CHECK so the
failure can be read, and changed nothing about the engine except adding the two readings.
