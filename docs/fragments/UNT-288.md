+++
id                = "UNT-288"
kind              = "unit"
status            = "closed"
cost              = 57541
closed            = "2026-09-12T21:11:03-04:00"
created           = "2026-09-12T20:53:20-04:00"
created_precision = "second"
supersedes        = []
blocks            = []
caused_by         = ["RLG-225"]
see_also          = []
delegate          = false
effort            = "medium"
model             = "claude-sonnet-5"
proposal          = "unobserved"
proposed_tier     = "broad_search"
ran_on            = "claude-opus-5"
routing           = "fix"
+++

### UNT-288 - The working cars get the working knob, in both games

- Routing class: `fix` -> **claude-sonnet-5**, effort **medium**. Bias 1. A known defect with a known cause. ([[RLG-092]])
- Opened for: [[RLG-225]]
- Opened by `unit.py`. **The identity exists before the first edit** ([[RLG-097]]), so the snapshot has somewhere to go.
- **CLOSED 2026-09-12T21:11:02-04:00** - v0.14.3. WORK_BODIES named six bodies and the fleet has eight working ones - AMBULANCE and HATCH were both added after the list was written. And only Interstate carried the body.workknob style: road.js toggles the class in both games because it cannot know which is running, which was harmless until RLG-213 made production a race class. Measured rather than assumed - garageBodies() in Motorsport returns the three production cars on main, so a SALOON reaches a circuit grid. tools/knob-test.py is new, runs both cabinets, reads the RENDERED knob rather than the class name, and takes BODY_CLASS as its oracle so it fails the day the engine's own list disagrees. Two falsifiers, both watched failing: --falsify knob fails question 2 in both games naming AMBULANCE and HATCH, --falsify circuit deletes the rule from Motorsport's live stylesheet and fails question 1 there. WORK_BODIES was deliberately NOT derived from BODY_CLASS - that is the unlock class, and RLG-227 is where the split gets made properly. RLG-227 and RLG-228 were tracked in the same commit and neither is started.
