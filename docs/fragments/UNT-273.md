+++
id                = "UNT-273"
kind              = "unit"
status            = "closed"
cost              = 38911
closed            = "2026-09-12T10:49:27-04:00"
created           = "2026-09-12T10:33:45-04:00"
created_precision = "second"
supersedes        = []
blocks            = []
caused_by         = ["RLG-213"]
see_also          = []
delegate          = false
effort            = "medium"
model             = "claude-sonnet-5"
ran_on            = "claude-opus-5"
routing           = "feature"
+++

### UNT-273 - The fleet resorts: production is the low-end race class and everything else that is not a racer is one class

- Routing class: `feature` -> **claude-sonnet-5**, effort **medium**. Bias 1. Implementation to a spec that already answers the design questions. ([[RLG-092]])
- Opened for: [[RLG-213]]
- Opened by `unit.py`. **The identity exists before the first edit** ([[RLG-097]]), so the snapshot has somewhere to go.
- **CLOSED 2026-09-12T10:49:27-04:00** - RLG-213's fleet resort, v0.13.88 - the first of its three pieces, with the ladder and the formula novelty left out. Production is the low-end race class and the player starts in it, so a class and an unlock became two questions: the row stays in BODY_CLASS and OPEN_FROM_THE_START says it costs nothing. Dropping the row was tried first and would have made a production car indistinguishable from a sports car, which the ladder has to tell apart. The cab and the pickup leave production, reversing RLG-114, and join the utility vehicles in one class called traffic - the flag from the retired hundred-mile rule, so no migration table was needed for it. But a save written since 2026-08-29 holds production and utility and lost every car it had won, measured at three of three, so OLD_UNLOCK_NAMES reads either old name as the new one and is never removed. Production stays RACE_BANNED until classOf has a production league, so every commit in between ships. THE CHECK WAS VACUOUS AND ONLY THE FALSIFICATION FOUND IT: it read each car's card for ??? and passed with the migration deleted, because a secret class is ABSENT from the garage when locked rather than shown, so setBody fell back to a ROADSTER and the card read ROADSTER. It reads garage membership now and fails correctly. The owner added three things mid-unit that belong to the ladder and are recorded rather than built: the third production car is a HATCHBACK and does not exist, production racers inherit the race paints while the same bodies stay in traffic, and the sports cars are locked on a fresh save - which cannot land before the production tournament exists or a new save has no road out.
