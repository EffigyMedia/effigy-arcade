+++
id                = "UNT-275"
kind              = "unit"
status            = "closed"
cost              = 14674
closed            = "2026-09-12T11:27:36-04:00"
created           = "2026-09-12T11:20:14-04:00"
created_precision = "second"
supersedes        = []
blocks            = []
caused_by         = ["RLG-213"]
see_also          = []
delegate          = false
effort            = "medium"
model             = "claude-sonnet-5"
ran_on            = "claude-opus-5"
routing           = "feature"
+++

### UNT-275 - A formula car has no league and is told which one to enter

- Routing class: `feature` -> **claude-sonnet-5**, effort **medium**. Bias 1. Implementation to a spec that already answers the design questions. ([[RLG-092]])
- Opened for: [[RLG-213]]
- Opened by `unit.py`. **The identity exists before the first edit** ([[RLG-097]]), so the snapshot has somewhere to go.
- **CLOSED 2026-09-12T11:27:36-04:00** - RLG-213's formula novelty, v0.13.90, the last of its three pieces. classOf still answers formula and it now means the opposite of what it meant: not that the car has a league of its own but that it has none. ENTRY_CLASSES holds production, sports and super, the choice is persisted and validated on every read, and racerBodies fields the entered class for a formula car and its own for everything else. The ENTER button is the one piece of new interface the whole ladder needed, and it is hidden rather than greyed for every other car - which breaks RLG-115's rule on purpose, because MODE is shut for a van to explain a rule the player meets again and a class choice for a car that has a class is not a rule but nothing. A formula win still pays the iridescent paints and never pays a rung, so the car cannot climb the ladder it sits on top of - that is the least-change reading and it stays inside the open reward question. I WROTE THE SAME FAULT TWICE IN ONE SESSION: the cycle step existed in the button and again in the test affordance, exactly as the gold probe had been a copy of the finish branch an hour earlier. cycleEntryClass is the one step now. The check reads the GRID rather than the setting and was watched failing with the formula league restored - all three arms reported entering production against VECTOR, APEX and COMET.
