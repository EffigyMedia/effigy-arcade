+++
id                = "EVD-756"
kind              = "evidence"
status            = "recorded"
step              = "tunnel-test still passes with the tube drawn span by span"
unit              = "UNT-303"
command           = "P:\\Code_Continuum\\Projects\\In-Dev\\Effigy_Arcade\\.venv\\Scripts\\python.exe tools/tunnel-test.py"
exit_code         = 0
duration_ms       = 33070
output_bytes      = 3933
output_sha        = "13febf9894a1a0a80e3eccd840eb6c33"
store_stamp       = "13c68ffe2365e78c"
created           = "2026-09-13T13:54:18-04:00"
created_precision = "second"
supersedes        = []
blocks            = []
caused_by         = []
see_also          = []
+++

### EVD-756 — step `tunnel-test still passes with the tube drawn span by span` of [[UNT-303]]

- Ran: `P:\Code_Continuum\Projects\In-Dev\Effigy_Arcade\.venv\Scripts\python.exe tools/tunnel-test.py`
- Result: **exit 0**, in 33070 ms
- Output: 3933 bytes, sha256 `13febf9894a1a0a80e3eccd840eb6c33`
- Store stamp at the time: `13c68ffe2365e78c`

**Output** — head only; the digest above covers the whole of it, so this excerpt cannot be selective without saying so.

```
tunnel-test  .  dark by day, through the one lamp clock
  browser: installed chrome - no bundled chromium, so the engine version is whatever this machine has

  DARK BY DAY, WHICH NOTHING ELSE ON THE BOARD IS
      midday in a TUNNEL:   lamps 1.00   (the day clock alone says 0.00)
      midday in FARMLAND:   lamps 0.00
      midnight in FARMLAND: lamps 1.00
  ok    the lamps are FULL ON at midday in a tunnel
  ok    and off at the same hour anywhere with a sky, so it is the PLACE
  ok    and the clock still says midday, so the place did not move the sun

  AND IT ARRIVES THROUGH THE ONE LAMP CLOCK, NOT A SECOND ROUTE
  ok    the shared clock returns full lamps while its own day term is zero
      midday in FARMLAND, heavy rain: lamps 1.00
  ok    and weather still lights them too, so the place is a term and not an override

  AND A ROOF MEANS NO WEATHER, WHICH FALLS OUT RATHER THAN BEING A RULE
      precip 0.00, canRain False, canSnow False
  ok    a tunnel precipitates on none of its rolls
  ok    and it can do neither, which comes from a precipitation of zero rather than a branch
  ok    and 600 rolls of the engine own weather produce nothing at all

  AND THE MOUTH IS A RAMP RATHER THAN A CUT, which is the comfort answer
      the crossing ran from 0 to 1.00 of the weather band
      driving OUT of a tunnel into daylight: 1.00 1.00 1.00 1.00 1.00 1.00 1.00 1.00 0.83 0.69 0.56 0.39 0.25 0.11
  ok    the run really did cross from full dark to full daylight
      the fade took about 0.4 second(s) of driving at nine tenths of top speed
  ok    and it passes through intermediate values rather than cutting
  ok    no single step is most of the way, which is what makes it comfortable

  AND IT HAS NO SKYLINE, BECAUSE THE PLACE HAS NO DISTANCE IN IT
      horizon forms: {'COASTAL': 'none', 'FARMLAND': 'open', 'TUNNEL': 'none', 'SWAMP': 'wetTree', 'JUNGLE': 'canopy', 'FOREST': 'tree', 'DESERT': 'mesa', 'MOUNTAIN': 'peak', 'CITY': 'tower', 'TUNDRA'
  ok    a tunnel states that it draws no horizon at all
  ok    and farmland states the open sky the owner asked for
  ok    and every place states a form, so nothing gets towers by fall-through

  AND A RUN CANNOT OPEN IN ONE, BUT THE ROAD MUST STILL REACH IT (RLG-140)
      passages:      ['TUNNEL', 'BRIDGE']
      a run may open in 10 of the 12 places
  ok    the tunnel and the bridge are the passages on the board
... 25 more line(s), covered by the digest
```

*Written by `step.py` as a byproduct of running the step. **Nobody attested to this** ([[RLG-081]]).*
