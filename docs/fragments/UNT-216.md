+++
id                = "UNT-216"
kind              = "unit"
status            = "closed"
cost              = 147415
closed            = "2026-09-08T19:47:33-04:00"
created           = "2026-09-08T18:30:19-04:00"
created_precision = "second"
supersedes        = []
blocks            = []
caused_by         = []
see_also          = []
delegate          = false
effort            = "medium"
model             = "claude-sonnet-5"
ran_on            = "claude-opus-5"
routing           = "feature"
+++

### UNT-216 - A speed trap is never spent on a racer

- Routing class: `feature` -> **claude-sonnet-5**, effort **medium**. Bias 1. Implementation to a spec that already answers the design questions. ([[RLG-092]])
- Opened by `unit.py`. **The identity exists before the first edit** ([[RLG-097]]), so the snapshot has somewhere to go.
- **CLOSED 2026-09-08T19:47:33-04:00** - One engagement, one target, and a trap that is actually parked

**THE HARNESS WAS PROVED TO FAIL BEFORE IT WAS TRUSTED.** `tools/commit-test.py` carries the
engine's own debug hooks, so it cannot simply be run against the previous commit - it throws on
a missing function rather than failing a check, which proves nothing. Instead the two guards it
asserts were switched off in place, leaving the hooks: `parked` forced false, and the
commitment gate removed from the retarget sweep. **Six of its checks failed**, each for the
reason it was written for - a trap drifting 3,967 units off its post, creeping at the 2,000-unit
speed floor, holding a target for 229 frames, and a cruiser reaching the player with no fresh
commitment while its `commits` counter ran from 2 to 6. The build was then restored and the file
verified free of the falsification markers.
