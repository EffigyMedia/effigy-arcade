+++
id                = "EVD-1129"
kind              = "evidence"
status            = "recorded"
step              = "slip-test: the lateral block is untouched"
unit              = "UNT-393"
command           = "P:/Code_Continuum/Projects/In-Dev/Effigy_Arcade/.venv/Scripts/python.exe tools/slip-test.py"
exit_code         = 0
duration_ms       = 35363
output_bytes      = 2322
output_sha        = "ebe68015b9aac68fe065f419a62ab730"
store_stamp       = "2167c44eb4e944c0"
created           = "2026-09-20T10:38:40-04:00"
created_precision = "second"
supersedes        = []
blocks            = []
caused_by         = []
see_also          = []
+++

### EVD-1129 — step `slip-test: the lateral block is untouched` of [[UNT-393]]

- Ran: `P:/Code_Continuum/Projects/In-Dev/Effigy_Arcade/.venv/Scripts/python.exe tools/slip-test.py`
- Result: **exit 0**, in 35363 ms
- Output: 2322 bytes, sha256 `ebe68015b9aac68fe065f419a62ab730`
- Store stamp at the time: `2167c44eb4e944c0`

**Output** — head only; the digest above covers the whole of it, so this excerpt cannot be selective without saying so.

```
slip-test  .  a wet road carries you further than you asked
  browser: installed chrome - no bundled chromium, so the engine version is whatever this machine has

  WHAT THE ROAD TAKES AWAY, dialled back (RLG-132)
      grip:    dry 1.00   heavy snow on covered ground 0.34   heavy rain on water 0.52
  ok    a wet road takes something, which is the hindrance the owner wants kept
  ok    and snow is still worse than rain, which is worse than dry
      (heavy snow lands ON the floor of 0.34)
      braking: dry 1.00   heavy snow 0.34   heavy rain 0.58
  ok    braking grip is simply reduced by the wetness, and snow reduces it most
  ok    and it is a plain reduction with no delta behaviour in it, exactly as ruled

  IT CARRIES YOU FURTHER THAN YOU ASKED, IN PROPORTION TO THE MOVEMENT
      on snow: asking for 0.700 rests at 0.8756, asking for 0.175 rests at 0.2189
  ok    a large movement on snow carries the car well past the mark
  ok    and a small one costs far less, which is what steering discreetly buys
  ok    and it is PROPORTIONAL rather than just smaller, which is what makes it knowable
      and three seconds later the car is still at 0.8756
  ok    the overshoot STAYS, so aiming short can be relied on rather than waited out

  SO YOU CAN UNDERSTEER ON PURPOSE AND ARRIVE EXACTLY, which is the owner point
      snow at slick 0.66: aim 0.5596 to arrive at 0.700, landed at 0.7000
  ok    understeering by the amount the surface adds lands the car ON the mark

  SNOW IS A DIFFERENT CURVE, AND A DRY ROAD IS THE CONTROL
      the same movement: snow overshoots 0.1756, rain 0.0647, dry 0.0042
  ok    rain carries the car too, so it is one model with two multipliers
  ok    and snow throws it much harder than rain rather than a little
  ok    and a dry road carries it nowhere, so every reading above is the weather

  AND A SLICK ROAD RUNS YOU WIDE IN A BEND
      the same bend, same speed: dry runs to -0.060, snow runs to -0.276
  ok    a bend pushes the car toward the outside even on a dry road
  ok    and a slick road carries it MARKEDLY wider, which is what the owner asked for
  ok    and to the same side, so it is the same bend doing it rather than two effects
  ok    and the slip itself is carrying part of it, not just the cornering force
  ok    no page errors

all checks passed
```

*Written by `step.py` as a byproduct of running the step. **Nobody attested to this** ([[RLG-081]]).*
