+++
id                = "RLG-295"
kind              = "ruling"
type              = "concept"
status            = "in-flight"
created           = "2026-09-20T09:12:33-04:00"
created_precision = "second"
source            = "owner 2026-09-20"
supersedes        = []
blocks            = ["RLG-299"]
caused_by         = []
see_also          = []
+++

### RLG-295 — Double the draw distance forward
- **RULED 2026-09-20T09:12:48-04:00** — OWNER, 2026-09-20, from the device: 'I think we may need to double the draw distance forward.' DRAW is 150 segments (30,000 units) and the note on it records that it was raised from 95 because the road stopped short of the horizon. Doubling it is not one constant: the road pass walks every slice each frame, the sprite buckets and the hill clip are built over it, the far road band begins where it ends, and the skyline's growth is measured against it. MEASURE THE FRAME RATE BEFORE AND AFTER with fps-test, on the phone's own terms, and read what else reads DRAW before changing it. Not started.
- **RULED 2026-09-20T12:44:41-04:00** — MEASURED 2026-09-20, NOT CHANGED. This ruling's own instruction is to measure the frame rate before and after and to read what else reads DRAW before changing it. Both are done and the answer needs the owner, because doubling the draw is not free and it is not one constant.

THE FRAME COST, measured with tools/draw-cost.py (EVD-1143). `DRAW` is a `const` read in over a hundred places and cannot be toggled at runtime, so the engine is SERVED with the one line rewritten and the two builds are ALTERNATED in one page, reloading between arms - two sequential fps-test runs cannot be compared on this machine, which was demonstrated twice today. At 55 per cent of top speed, midday, no weather:

  MOUNTAIN   150 (30,000 units) 60.7-60.8    300 (60,000 units) 38.4-53.7
  CANYON     150 (30,000 units) 60.6-60.7    300 (60,000 units) 46.8-58.6
  FOREST     150 (30,000 units) 60.7-60.7    300 (60,000 units) 51.3-60.7

A second run agreed: MOUNTAIN 60.6-60.7 against 36.5-60.6, CANYON 51.4-60.7 against 38.2-59.5, FOREST 60.7 against 60.6-60.8. The two ranges do not overlap for the mountain or the canyon, which is fps-test's own rule for a real change. THIS IS A DESKTOP, and RLG-002 makes this a phone game.

THE PLACES THAT COST THE MOST ARE THE ONES WITH A SURFACE PAINTED PER SLICE - the mountain's drop and the canyon's walls, both of which are a quad a slice a side. The forest, which has neither, pays least. So the cost is not the road pass alone, and it will grow with every surface added beside the road.

WHAT ELSE READS DRAW, which is the other half of the instruction. It is genuinely 'the far end of the draw' nearly everywhere, and four of those are behaviour rather than drawing:
- `OUT_OF_SIGHT` is `DRAW*SEG + 5000` and is where deer, traffic, crates, police and road furniture are SPAWNED. It must follow the draw or a car spawns in view and pops in, so this coupling is correct and unavoidable - but every spawn distance doubles with it.
- `RADAR_RANGE` IS `OUT_OF_SIGHT`, so the radar detector's warning distance doubles too. That is a FEEL number the owner has already been asked about once - 'whether a quarter of a mile is enough warning' - and doubling it is a gameplay change nobody asked for. It can be pinned instead.
- The PLACE PLANNER plants the next biome at `pos/SEG + DRAW`, so a place is chosen and its edge is set twice as far out.
- `skyRiseOf` derives the skyline's height cap from `CAM_D / (DRAW*SEG)`, so doubling the draw HALVES the cap. That is correct by RLG-104's own reasoning - the furthest thing the road pass draws is smaller on screen, so the horizon may not be taller - but it visibly shortens the canyon's skyline band.
- The ARRIVAL RAMP is `1 - n/DRAW` and everything reads it (RLG-218), so the fade band doubles in world units and stays the same fraction. That one is right.
- `SIREN_LOOK_MAX` is `DRAW*SEG`, so a siren's cap doubles.

SO THE DECISION IS THE OWNER'S AND IT IS THREE QUESTIONS: whether to pay 60 to about 40 fps in the mountain and the canyon for twice the road; whether the radar detector's range should double with it or be pinned where it is; and whether 300 is the number or something between. Nothing was changed.
- **RULED 2026-09-20T13:19:13-04:00** — BUILT 2026-09-20, v0.14.104. OWNER, shown the measurement above: 'Let's do 300 and I'm gonna have to optimize like we were planning on two anyways.' DRAW is 300 - 60,000 units - and the radar detector is PINNED where it was, on the owner's separate ruling.

THE RADAR WAS `OUT_OF_SIGHT`, which is the draw plus 5,000, and the note on it explains why that was right: a detector reaching exactly as far as police can first exist can never have one appear inside its cone with no approach. Doubling the draw would have doubled the warning, and how much warning it gives is a FEEL number the owner tunes rather than one that moves because something unrelated moved. It keeps the 35,000 it had, and the original argument still holds in the direction that matters - it is no LARGER than the spawn horizon.

WHAT IT COSTS, at v0.14.104 by fps-test at midday against the same harness at DRAW 150 earlier the same day: CITY 58.0-60.4 to 38.8-41.6, FOREST 59.6-60.0 to 50.4-52.8, MOUNTAIN 55.2-60.0 to 33.2-56.4, COASTAL 57.6-60.0 to 28.4-60.0, SWAMP 60.0 to 49.2-60.0. THE DESERT AND THE TUNDRA DID NOT MOVE - 59.6-60.4 to 60.0-60.4 and 58.8-60.0 to 60.0 - which says the cost is what is drawn BESIDE the road rather than the road pass walking twice as many slices. RLG-299 is the optimisation the owner named and it holds the table.

TWO HARNESSES HELD THEIR OWN COPY OF SOMETHING THE ENGINE OWNS, and both were found by doubling the draw rather than by reading. biome-test asserted the new place is planted 'a draw distance ahead' against a LITERAL 150, and failed with the planner behaving perfectly - the edge 298 segments ahead of a car at 2165. It reads `API.drawSegments` now, which was published for it. farroad-test's join probe called `.toFixed` on a `dist()` that already answers null when a sample comes back empty, so the whole harness died with a TypeError instead of reporting the frame it could not read, the moment the rows it samples at moved.

AND THE WALL'S FOOT STEPPED AT EVERY CREST once there were twice as many crests to see it at (RLG-297). The band started at a remembered seam x with a bottom computed for its own far end, which is a different distance after a skip. The seam carries the bottom now, the way it already carried the top.

occlusion-test's LAMP line clipped 0 of 46,337 on one run and 1,899 of 46,264 on the next, which is the harness the record already says never to quote a single run of. The tree line, which has ten times the sample, passes robustly at 188,684 of 578,043.

wall-plane-test, radar-test, pop-test, skyline-test, biome-test and farroad-test pass; smoke-test 7/7; drive-test 36/37 on the recorded settle flake at 97.7 per cent.
- **RULED 2026-09-20T14:22:41-04:00** — THE DOUBLED DRAW EMPTIED THE ROAD, AND THE OWNER FOUND IT ON THE DEVICE, NOT ME (2026-09-20, fixed in v0.14.106). 'When I just played there was no traffic.' MEASURED: at DRAW 300 the car count sat at ELEVEN for a whole run and never moved; at DRAW 150 it ran 12 to 20. Eleven is exactly the seed laid down at reset - nothing spawned after the run started.

THE MECHANISM IS THREE LITERALS THAT WERE ALL 'JUST PAST THE DRAW' WHEN THE DRAW WAS 30,000. A wave is placed at `OUT_OF_SIGHT`, which became 65,000; the forward cull was a LITERAL `pos + 64000`; and the spawner's own horizon was a literal `pos + 62000`. So every wave was born ONE THOUSAND UNITS PAST THE POINT WHERE CARS STOP EXISTING and was culled on the same frame. A fourth, the reset seed, filled only to 52,000 of a 60,000 road; a fifth, the speed trap's `rnd(OUT_OF_SIGHT, 52000)`, had its range INVERTED, because `rnd(a,b)` is `a + random()*(b-a)`.

WHAT I GOT WRONG, AND IT IS THE POINT OF THIS ENTRY. The ruling told me to 'read what else reads DRAW before changing it' and I did - all 109 references, and the survey in the entry above is correct as far as it goes. IT IS THE WRONG SEARCH. Nothing that broke mentions DRAW. They are numbers that were CHOSEN against the draw and then written down, which no search for the identifier can find, and no harness covered because the road still looked right - the seeded traffic is in front of you for the first mile. A CONSTANT'S DEPENDENTS ARE NOT ITS REFERENCES.

OWNER, on being told: 'It looks like we might need to find every instance of things tied to the draw distance and tie them together in a way that modifying one modifies the rest of them.' Done, and it is one block beside the draw itself: ROAD_FAR, OUT_OF_SIGHT, SPAWN_AHEAD, WAVE_AHEAD, CULL_AHEAD, GEN_AHEAD, LOOK_AHEAD, BORE_FAR, SKY_BEND_AT. The rule that matters is written where it can be read - the culler must be FURTHER OUT than the spawner, which a literal cannot hold and these can.

WHAT IS DELIBERATELY NOT IN IT, because tying it in would be the same mistake in the other direction: `RAGE_LOST` is how far you have to outrun an angry driver, the cloud layers' near and far are a sky, the coast's `shore` is how far out the sea starts, and `MIRROR_BACK` is behind you. None is a statement about how far the road is drawn.

MEASURED AFTER: traffic 13-21 at DRAW 300 against 8-19 at DRAW 150, which is the longer road carrying a few more cars as it should. traffic-test read a corridor of 0.358 with no closure; roadblock-test, deer-test, tunnel-test and skyline-test pass; smoke-test 7/7; drive-test 36/37 on the settle flake.
