+++
id                = "EVD-1029"
kind              = "evidence"
status            = "recorded"
step              = "contact-test with contact asked before the push"
unit              = "UNT-366"
command           = "P:\\Code_Continuum\\Projects\\In-Dev\\Effigy_Arcade\\.venv\\Scripts\\python.exe tools/contact-test.py"
exit_code         = 0
duration_ms       = 16708
output_bytes      = 1266
output_sha        = "d86ffffb5995b620c22aece47c1d9362"
store_stamp       = "ddb617f5a9df799c"
created           = "2026-09-18T12:22:37-04:00"
created_precision = "second"
supersedes        = []
blocks            = []
caused_by         = []
see_also          = []
+++

### EVD-1029 — step `contact-test with contact asked before the push` of [[UNT-366]]

- Ran: `P:\Code_Continuum\Projects\In-Dev\Effigy_Arcade\.venv\Scripts\python.exe tools/contact-test.py`
- Result: **exit 0**, in 16708 ms
- Output: 1266 bytes, sha256 `d86ffffb5995b620c22aece47c1d9362`
- Store stamp at the time: `ddb617f5a9df799c`

**Output** — head only; the digest above covers the whole of it, so this excerpt cannot be selective without saying so.

```
contact-test  .  a car you have hit is still solid

  browser: installed chrome - no bundled chromium, so the engine version is whatever this machine has
  RUN ONE - a stationary car dead ahead, driven into at speed
      165 frames, 131 of them nose-to-tail with the car
      closest the two centres came: 0.22 of the overlap width
  ok    the player actually reached the car   131 frames
  ok    the player never occupies the same point as the car it hit   0 frame(s) at under a tenth of the overlap while square to it
      speed on meeting it: 6893, slowest while square to it: 0
  ok    a car in front takes your speed rather than being driven through   0 down from 6893
  ok    and it costs a hit   1

  RUN TWO - a car alongside, with the wheel held into it for two seconds
      112 frames, 31 of them with the two bodies overlapping
      1 crash response(s)
  ok    the wheel held into it really does hold them together   31 frames of contact
  ok    leaning on a car costs a hit at all   1
  ok    but it is not one a frame   1 responses over 31 frames of contact
  ok    no page errors

  the car is solid and the crash fires once
  whether the shove FEELS right, and whether one hit is the right cost for a long
  grind, is the owner call on a device.
```

*Written by `step.py` as a byproduct of running the step. **Nobody attested to this** ([[RLG-081]]).*
