+++
id                = "EVD-1162"
kind              = "evidence"
status            = "recorded"
step              = "canyon-test: the cap checks read the wall now, and pass"
unit              = "UNT-404"
command           = "P:\\Code_Continuum\\Projects\\In-Dev\\Effigy_Arcade\\.venv\\Scripts\\python.exe tools/canyon-test.py"
exit_code         = 1
duration_ms       = 13431
output_bytes      = 3039
output_sha        = "65da90030feb21d6bfb2268efaddc61d"
store_stamp       = "b373c7f4ee58fcba"
created           = "2026-09-20T21:26:30-04:00"
created_precision = "second"
supersedes        = []
blocks            = []
caused_by         = []
see_also          = []
+++

### EVD-1162 — step `canyon-test: the cap checks read the wall now, and pass` of [[UNT-404]]

- Ran: `P:\Code_Continuum\Projects\In-Dev\Effigy_Arcade\.venv\Scripts\python.exe tools/canyon-test.py`
- Result: **EXIT 1 — the step FAILED**, in 13431 ms
- Output: 3039 bytes, sha256 `65da90030feb21d6bfb2268efaddc61d`
- Store stamp at the time: `b373c7f4ee58fcba`

**Output** — head only; the digest above covers the whole of it, so this excerpt cannot be selective without saying so.

```
canyon-test  .  the horizon is one mass, and it closes on itself
  browser: installed chrome - no bundled chromium, so the engine version is whatever this machine has
  ok    the canyon is on the board
  ok    and it states a continuous form rather than a row of objects

  THE SILHOUETTE OF EVERY PLACE, BY THE SAME THREE RULES
      place      form       gaps   step   seam
      FARMLAND   open        487      2     15
      SWAMP      wetTree      36     10      3
      JUNGLE     canopy        0      1      0
      FOREST     tree          3     16     28
      DESERT     mesa         82      4     55
      MOUNTAIN   peak          0      3    104
      CITY       tower         6    125     10
      TUNDRA     peak          0      4     16
      CANYON     ridge         0      2      1
      BRIDGE     open        485      2     16

  THE CANYON, AGAINST WHAT A CONTINUOUS FORM HAS TO BE
  ok    the canyon has a silhouette to read
  ok    no column of the wall reaches the ground - it has no gaps in it
  ok    and the crest walks rather than jumping, so it is not a comb
  ok    and it closes on itself, so the tiled seam is not a cliff

  AND THE SAME RULES ON EVERY OTHER PLACE, WHICH IS THE FALSIFICATION
      FARMLAND   breaks gaps,seam
      SWAMP      breaks gaps,step
      JUNGLE     breaks NOTHING
      FOREST     breaks gaps,step,seam
      DESERT     breaks gaps,step,seam
      MOUNTAIN   breaks seam
      CITY       breaks gaps,step,seam
      TUNDRA     breaks step,seam
      BRIDGE     breaks gaps,seam
  FAIL  no free-standing horizon passes all three, so the rules measure something   [these passed as continuous: ['JUNGLE']]
  FAIL  and the ones that are already gapless fail on the seam, which is the walk   [{'JUNGLE': 0, 'MOUNTAIN': 104, 'TUNDRA': 16}]

  AND A WALL CANNOT STAND BEHIND ITSELF
      canyon: skyline 38.0 px against a tallest wall of 38.0 px
  ok    the canyon has roadside walls to measure against
... 18 more line(s), covered by the digest
```

*Written by `step.py` as a byproduct of running the step. **Nobody attested to this** ([[RLG-081]]).*
