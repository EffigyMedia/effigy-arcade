+++
id                = "RVW-002"
kind              = "review"
status            = "recorded"
unit              = "UNT-417"
created           = "2026-09-22T10:46:39-04:00"
created_precision = "second"
source            = "review of UNT-417, scoped to `review.py --packet`"
supersedes        = []
blocks            = []
caused_by         = []
see_also          = ["UNT-417"]
files             = ["arcade.js", "road.js", "tools/glass-survey-test.py", "tools/range-drift-proof.py", "tools/range-peak-test.py", "tools/wall-plane-test.py"]
findings          = 5
packet            = "unpinned"
reviewed_lines    = 809
verdict           = "not-sound"
+++

### RVW-002 — review of [[UNT-417]] — not-sound

- **Scope**: 6 file(s), 809 code line(s). The `files` field is the diff at the moment this was filed, read from git rather than typed by the reviewer.
- **Packet**: not pinned. Nobody passed back the digest of the packet this was read from, so `files` is the boundary the review was scoped to and not proof of what was read. A code file edited between the two calls would be listed here unread.
- **Findings**: 5.

Fresh-eyes review of UNT-417 by a separate agent, of packet 311b3a8d8ffaa65d. Verdict: NOT SOUND. The peak model was consistent - floor formulas and kv agree in both views, alpha is restored, negative indices align, bioBehind works far back, no removed name survives - but four findings made output or a harness wrong. All four are fixed in the same unit before its commit, so this record is filed unpinned: the tree changed after the packet was read.

1. HIGH - peaks on slices inverted by under a pixel were never drawn, so they blinked out each frame the slice flipped; that is about half the far draw on a hilly road. FIXED: one helper, peakSlice, is called from all three branches of the walk.
2. HIGH - at the defaults a peak's top was RANGE_H*(1-RANGE_SINK) = 13 camera heights above the floor, exactly the floor's depth plus the eye, so no peak broke the horizon; in the glass the far loop's peaks were painted over entirely by the first step's floor while still counting as drawn, and the band between mFar and MIRROR_BACK was drawn twice. FIXED: RANGE_H is 28, a top about five camera heights above the eye, and range-peak-test now asserts a top above the horizon (measured 274 against 345); the far loop stops where the walk starts.
3. MEDIUM - glass-survey-test --falsify looked for a deleted line and exited. FIXED: it now cuts the glass's peaks in rangePeak.
4. MEDIUM - range-peak-test check 2 compared the painter's offset with itself and could fail only by editing that line. FIXED: the car is stopped and each drawn windscreen peak is compared with API.projAt of its world point, the engine's projection asked directly; the falsify arm shifts the drawn x by 12 px and fails exactly that check (12.00 px against 0.00).
5. LOW, left: the glass has no bend term, so bends add nothing for it; this is the glass's existing behaviour.
