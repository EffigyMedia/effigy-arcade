+++
id                = "EVD-1027"
kind              = "evidence"
status            = "recorded"
step              = "collide-test passes in full: grazing hits register, both bodies measure at their declared width"
unit              = "UNT-366"
command           = "P:\\Code_Continuum\\Projects\\In-Dev\\Effigy_Arcade\\.venv\\Scripts\\python.exe tools/collide-test.py"
exit_code         = 0
duration_ms       = 45169
output_bytes      = 1702
output_sha        = "83065a0c8502750444b9acf6117a47c3"
store_stamp       = "94fc11d0fd2a6269"
created           = "2026-09-18T12:21:32-04:00"
created_precision = "second"
supersedes        = []
blocks            = []
caused_by         = []
see_also          = []
+++

### EVD-1027 — step `collide-test passes in full: grazing hits register, both bodies measure at their declared width` of [[UNT-366]]

- Ran: `P:\Code_Continuum\Projects\In-Dev\Effigy_Arcade\.venv\Scripts\python.exe tools/collide-test.py`
- Result: **exit 0**, in 45169 ms
- Output: 1702 bytes, sha256 `83065a0c8502750444b9acf6117a47c3`
- Store stamp at the time: `94fc11d0fd2a6269`

**Output** — head only; the digest above covers the whole of it, so this excerpt cannot be selective without saying so.

```
collide-test  .  the hit is where the car is
  browser: installed chrome - no bundled chromium, so the engine version is whatever this machine has
  ok    the engine can park a car and report the damage
      parked a sedan: half-widths sum to 0.20860 of the road
  ok    a car in the same place as you is a hit
  ok    and one well clear of you is not
      the hit stops at a lateral offset of 0.2086
  ok    a hit fires when the cars overlap and stops when they do not
  ok    and it stops exactly at the sum of the two half-widths
      grazing overlaps: 0.012 hit, 0.008 hit, 0.004 hit, 0.002 hit, 0.001 hit
  ok    an overlap as shallow as a thousandth of the road is a hit
  ok    and two cars with daylight between them are not
      truck  (0.320 wide) collides at 0.2272
      coupe  (0.260 wide) collides at 0.2024
      the player therefore collides at 0.2298 of the road, drawn at 0.2300
      (diagnostic, not an assertion: the separation puts the player at 0.2298 against a drawn 0.2300 - see the note in the source)
  ok    the world runs again after a change of car (roadster)
  ok    the staged collision survives a change of car
      ROADSTER  hit edge 0.2066 against a declared sum of 0.2066 (its own width 0.2253)
  ok    the roadster collides at its own declared width
  ok    the world runs again after a change of car (muscle)
  ok    the staged collision survives a change of car
      MUSCLE    hit edge 0.2165 against a declared sum of 0.2165 (its own width 0.2491)
  ok    the muscle collides at its own declared width
  ok    and two different cars are two different widths
      readings retaken because the world stood still: 0
  ok    no page errors

all checks passed
```

*Written by `step.py` as a byproduct of running the step. **Nobody attested to this** ([[RLG-081]]).*
