+++
id                = "EVD-816"
kind              = "evidence"
status            = "recorded"
step              = "each stamp check passes when ROAD_BUILD and A.version agree and fails when either lags or a stamp line is missing, in pack.sh --check, smoke stamps(), and the smoke page's build tag"
unit              = "UNT-313"
command           = "../../../.venv/Scripts/python tools/stamps-falsify.py --browser"
exit_code         = 0
duration_ms       = 27186
output_bytes      = 645
output_sha        = "908ac40afc88ada95b47fae7fbc3487f"
store_stamp       = "c73aa2cd11b25365"
created           = "2026-09-13T17:45:41-04:00"
created_precision = "second"
supersedes        = []
blocks            = []
caused_by         = []
see_also          = []
+++

### EVD-816 — step `each stamp check passes when ROAD_BUILD and A.version agree and fails when either lags or a stamp line is missing, in pack.sh --check, smoke stamps(), and the smoke page's build tag` of [[UNT-313]]

- Ran: `../../../.venv/Scripts/python tools/stamps-falsify.py --browser`
- Result: **exit 0**, in 27186 ms
- Output: 645 bytes, sha256 `908ac40afc88ada95b47fae7fbc3487f`
- Store stamp at the time: `c73aa2cd11b25365`

**Output** — head only; the digest above covers the whole of it, so this excerpt cannot be selective without saying so.

```
PASS  stamps() passes when the stamps agree
PASS  pack.sh --check exits 0 when the stamps agree
PASS  pack.sh --check wrote nothing
PASS  stamps() fails when road.js lags
PASS  stamps() names both versions
PASS  pack.sh --check exits non-zero when road.js lags
PASS  pack.sh refuses because of the stamps
PASS  stamps() fails when arcade.js lags
PASS  pack.sh --check fails when arcade.js lags
PASS  stamps() fails when road.js has no stamp
PASS  pack.sh --check fails when road.js has no stamp
PASS  smoke-test exits non-zero on a drifted page
PASS  smoke flags the stamps row
PASS  smoke flags the MIXED build tag in the cabinet

0 failure(s)
```

*Written by `step.py` as a byproduct of running the step. **Nobody attested to this** ([[RLG-081]]).*
