+++
id                = "EVD-669"
kind              = "evidence"
status            = "recorded"
step              = "the ladder, the locked sports class, and the bottle rule, each watched failing with its own defect put back"
unit              = "UNT-274"
command           = "P:\\Code_Continuum\\Projects\\In-Dev\\Effigy_Arcade\\.venv\\Scripts\\python.exe tools/resort-test.py"
exit_code         = 0
duration_ms       = 31563
output_bytes      = 1135
output_sha        = "f6cb8d0976f63069694518729ff42b65"
store_stamp       = "7551d12195c00be2"
created           = "2026-09-12T11:15:00-04:00"
created_precision = "second"
supersedes        = []
blocks            = []
caused_by         = []
see_also          = []
+++

### EVD-669 — step `the ladder, the locked sports class, and the bottle rule, each watched failing with its own defect put back` of [[UNT-274]]

- Ran: `P:\Code_Continuum\Projects\In-Dev\Effigy_Arcade\.venv\Scripts\python.exe tools/resort-test.py`
- Result: **exit 0**, in 31563 ms
- Output: 1135 bytes, sha256 `f6cb8d0976f63069694518729ff42b65`
- Store stamp at the time: `7551d12195c00be2`

**Output** — head only; the digest above covers the whole of it, so this excerpt cannot be selective without saying so.

```
resort  .  production is a race class and the non-racers are one class
  browser: installed chrome - no bundled chromium, so the engine version is whatever this machine has
  ok    a new player STARTS in production - both cars, nothing to unlock
  ok    and has none of the merged class yet
  ok    the old hundred-mile flag still opens the WHOLE merged class
  ok    a save holding only 'utility' keeps every car it had won
  ok    a save holding only 'production' keeps every car it had won
  ok    no body is given two classes by the sheet
  ok    the sheet agrees with the garage about the merged class
  ok    and about production
  ok    a fresh save does NOT hold the sports cars
  ok    a production car enters the PRODUCTION league
  ok    and it may actually enter a race
      with a SALOON the MODE control reads 'TEST DRIVE'
  ok    a gold in production unlocks the sports class
  ok    and the whole ladder runs production, sports, super, formula
  ok    a production car carries NO nitrous bottle
  ok    and the sports cars still do - so the check is paired

the fleet sorts into three groups and both class maps agree
```

*Written by `step.py` as a byproduct of running the step. **Nobody attested to this** ([[RLG-081]]).*
