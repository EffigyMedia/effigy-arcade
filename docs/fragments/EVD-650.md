+++
id                = "EVD-650"
kind              = "evidence"
status            = "recorded"
step              = "the harness prints FLOOR ONLY with its reason beside every distance, and the withdrawal validates"
unit              = "UNT-269"
command           = "P:\\Code_Continuum\\Projects\\In-Dev\\Effigy_Arcade\\.venv\\Scripts\\python.exe tools/reach-test.py --bodies TUNER --runs 1 --cap 200"
exit_code         = 0
duration_ms       = 195104
output_bytes      = 1099
output_sha        = "57714e1a349407a23ef40b56a4a2dc4d"
store_stamp       = "46b0aa09ed0c98e6"
created           = "2026-09-12T08:54:36-04:00"
created_precision = "second"
supersedes        = []
blocks            = []
caused_by         = []
see_also          = []
+++

### EVD-650 — step `the harness prints FLOOR ONLY with its reason beside every distance, and the withdrawal validates` of [[UNT-269]]

- Ran: `P:\Code_Continuum\Projects\In-Dev\Effigy_Arcade\.venv\Scripts\python.exe tools/reach-test.py --bodies TUNER --runs 1 --cap 200`
- Result: **exit 0**, in 195104 ms
- Output: 1099 bytes, sha256 `57714e1a349407a23ef40b56a4a2dc4d`
- Store stamp at the time: `46b0aa09ed0c98e6`

**Output** — head only; the digest above covers the whole of it, so this excerpt cannot be selective without saying so.

```
reach  .  how far ONE timed test drive gets before the clock ends it
  browser: installed chrome - no bundled chromium, so the engine version is whatever this machine has

  FALSIFIER - can this harness report a LONG run at all?
      TUNER with the speed pinned at 1,000mph, timer ON
        15.74 miles in 90s of wall clock, clock 60.0 -> 112.6 (lowest 49.9)
  ok    the harness reports a run twice past the timed ceiling, and the clock CLIMBS
        and it says NOTHING about whether this driver is representative.
        It is not: read the budget at the top of this file.

  HOW FAR DOES A CAR GET FLAT OUT WITH NOBODY STEERING - A FLOOR
      checkpoints are 2 miles apart and pay 20s, a crate pays 10s,
      and the run starts with 60s on the clock
      TUNER        3.85 miles   the clock ran out after  93s   1 crate(s), peak 146 mph

the falsifier cleared its bar, so the distances above are a REAL FLOOR -
and a floor is all they are. A player steers to the crates and reaches the
car's own top end, and the budget at the top of this file is what says how
far THAT goes. See RLG-215.
```

*Written by `step.py` as a byproduct of running the step. **Nobody attested to this** ([[RLG-081]]).*
