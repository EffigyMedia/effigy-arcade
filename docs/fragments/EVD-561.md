+++
id                = "EVD-561"
kind              = "evidence"
status            = "recorded"
step              = "a trap stays parked, a cruiser holds one target, and it parks again when beaten"
unit              = "UNT-216"
command           = ".venv/Scripts/python.exe tools/commit-test.py"
exit_code         = 127
duration_ms       = 7
output_bytes      = 69
output_sha        = "32d13edb610e49a5ef8a9e774e413581"
store_stamp       = "485c0306c83b4eb4"
created           = "2026-09-08T19:41:36-04:00"
created_precision = "second"
supersedes        = []
blocks            = []
caused_by         = []
see_also          = []
+++

### EVD-561 — step `a trap stays parked, a cruiser holds one target, and it parks again when beaten` of [[UNT-216]]

- Ran: `.venv/Scripts/python.exe tools/commit-test.py`
- Result: **EXIT 127 — the step FAILED**, in 7 ms
- Output: 69 bytes, sha256 `32d13edb610e49a5ef8a9e774e413581`
- Store stamp at the time: `485c0306c83b4eb4`

**Output** — head only; the digest above covers the whole of it, so this excerpt cannot be selective without saying so.

```
could not run: [WinError 2] The system cannot find the file specified
```

*Written by `step.py` as a byproduct of running the step. **Nobody attested to this** ([[RLG-081]]).*

**THIS RUN NEVER HAPPENED.** Exit 127 is the shell failing to find the interpreter, not the
check failing: the harness needs the venv python named as `.venv/Scripts/python.exe` by an
absolute path when it is launched through the step runner. The real run is [[EVD-562]].
