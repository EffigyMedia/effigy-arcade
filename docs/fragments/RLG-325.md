+++
id                = "RLG-325"
kind              = "ruling"
type              = "defect"
status            = "requested"
created           = "2026-09-22T11:55:48-04:00"
created_precision = "second"
source            = "found 2026-09-22, UNT-420"
supersedes        = []
blocks            = []
caused_by         = []
see_also          = ["RLG-300", "RLG-306"]
+++

### RLG-325 — hazard-test fails five mountain checks on the committed engine: loose rock, its mirror, and the valley floor
- **RULED 2026-09-22T11:56:02-04:00** — FOUND, NOT FIXED. tools/hazard-test.py fails five checks, all on the MOUNTAIN, and fails them identically on the committed engine (0.14.126) with its own committed copy - so the swamp change in UNT-420 did not cause them, and no recorded run shows when they started:

  'the solid side of a mountain still carries its rock' - 0 objects (twice, once per hazard side)
  'and the mirror carries rock on the solid side and nothing on the drop' - 0 and 0 (twice)
  'MOUNTAIN: past the rim lies a floor far below, not ground at the road level' - floorN None, with 65 to 143 floor slices traced

FIRST READING, NOT A DIAGNOSIS. The first four look STALE, not broken: RLG-300 removed the loose ROCKFACE sprites from any side with a sheer face, and RLG-306 made the mountain's solid side one low-polygon mass - so 'rock objects on the solid side' is now asked of a side that deliberately has none. If so the checks should ask for the mass there instead (mass-proof already covers the mass). The fifth is different and must be read before it is called stale: the floor IS traced (dropTrace.floor has entries) but the check found no floor at the row it samples, which could be the harness sampling a row the mass now covers, or a real floor missing at that distance.
