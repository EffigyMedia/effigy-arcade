+++
id                = "EVD-715"
kind              = "evidence"
status            = "recorded"
step              = "the deployed version read from the remote rather than from the thread"
unit              = "UNT-292"
command           = "P:/Code_Continuum/Projects/In-Dev/Effigy_Arcade/.venv/Scripts/python.exe -c \nimport subprocess\nv = subprocess.run(['git','show','origin/main:arcade.js'],capture_output=True,text=True).stdout\nline = [l for l in v.splitlines() if 'A.version' in l][0].strip()\nn = subprocess.run(['git','log','--oneline','origin/main..main'],capture_output=True,text=True).stdout.strip().splitlines()\nprint('deployed:', line)\nprint('unpushed commits:', len(n))\nassert '0.14.1' in line, 'the remote is not where this was measured'\nassert len(n) == 5, 'the gap is not five commits'\nprint('MEASURED - the claim of v0.13.89 and sixteen builds was false')\n"
exit_code         = 0
duration_ms       = 352
output_bytes      = 114
output_sha        = "0f39ce1c7acf8633e2d5c1cdc60a6ea4"
store_stamp       = "7177d26a78f89830"
created           = "2026-09-12T23:09:06-04:00"
created_precision = "second"
supersedes        = []
blocks            = []
caused_by         = []
see_also          = []
+++

### EVD-715 — step `the deployed version read from the remote rather than from the thread` of [[UNT-292]]

- Ran: `P:/Code_Continuum/Projects/In-Dev/Effigy_Arcade/.venv/Scripts/python.exe -c 
import subprocess
v = subprocess.run(['git','show','origin/main:arcade.js'],capture_output=True,text=True).stdout
line = [l for l in v.splitlines() if 'A.version' in l][0].strip()
n = subprocess.run(['git','log','--oneline','origin/main..main'],capture_output=True,text=True).stdout.strip().splitlines()
print('deployed:', line)
print('unpushed commits:', len(n))
assert '0.14.1' in line, 'the remote is not where this was measured'
assert len(n) == 5, 'the gap is not five commits'
print('MEASURED - the claim of v0.13.89 and sixteen builds was false')
`
- Result: **exit 0**, in 352 ms
- Output: 114 bytes, sha256 `0f39ce1c7acf8633e2d5c1cdc60a6ea4`
- Store stamp at the time: `7177d26a78f89830`

**Output** — head only; the digest above covers the whole of it, so this excerpt cannot be selective without saying so.

```
deployed: A.version = '0.14.1';
unpushed commits: 5
MEASURED - the claim of v0.13.89 and sixteen builds was false
```

*Written by `step.py` as a byproduct of running the step. **Nobody attested to this** ([[RLG-081]]).*
