+++
id                = "RLG-251"
kind              = "ruling"
type              = "change"
status            = "in-flight"
created           = "2026-09-15T01:37:08-04:00"
created_precision = "second"
source            = "owner, 2026-09-15"
supersedes        = []
blocks            = []
caused_by         = []
see_also          = []
+++

### RLG-251 — Returning to the garage from any mode shows the car last driven
- **RULED 2026-09-15T01:38:09-04:00** — TRACKED, NOT STARTED. Owner, 2026-09-15: "When you return to the garage from any mode, it should return you to the car you last used."

WHAT "ANY MODE" MUST COVER: TEST DRIVE, SINGLE RACE, TOURNAMENT and INTERCEPT, each left by finishing, by wrecking or being busted, and by quitting from pause; and a reward screen that opens between the run and the garage (`showUnlock` loads the won car into `optBody` to draw it). The car is saved by key in `interstate-opts.body`.

NOT YET MEASURED: which of those paths lands on a different car today. Candidates: `showUnlock` not restoring on every exit, `enforceCarRules` moving the player when a mode and a car disagree, and a reboot reading a stale save. Measure each path with a harness before changing anything, and make the harness the proof.

INTERACTION WITH RLG-250: a fresh save opens on SALOON; a player who has driven opens on the car they last drove, even if that is mid-list.
- **RULED 2026-09-16T02:07:43-04:00** — STARTED AND NOT FINISHED, 2026-09-16, and this is where it stopped.

MEASURED SO FAR, with the real buttons: PAUSE > QUIT KEEPS THE CAR. A test drive in ROADSTER, paused and quit, lands in the garage on ROADSTER. That is one of the four doors the ruling names, and it is the one the fragment suspected least.

NOT YET MEASURED: the end screen's CHANGE CAR, the end screen's MAIN MENU, and a reload. tools/garage-return-test.py has all four arms written and only the first runs: each door leaves the player on a DIFFERENT screen - QUIT lands in the garage, the end screen carries its own buttons, MAIN MENU goes to the title - and the helper that walks back to the garage cannot yet find its way from all of them. The file says so in its own docstring and is NOT in the gate.

THE NEXT SESSION SHOULD FINISH THE NAVIGATION FIRST and let the measurement say which doors are broken, before touching `showUnlock` or `enforceCarRules`. No engine change has been made for this ruling.
- **RULED 2026-09-16T02:19:12-04:00** — MEASURED 2026-09-16, AND NO DOOR OUT OF AN INTERSTATE RUN LOSES THE CAR. `tools/garage-return-test.py` walks each door with the real buttons and reads the car back out of `API.body()`: END > CHANGE CAR drove COUPE and the garage showed COUPE, END > MAIN MENU drove SALOON and showed SALOON, PAUSE > RESTART drove HATCH and showed HATCH. Each arm picks a car the garage did NOT open on, and the three arms landed on three different cars in one run, which is the garage remembering across the whole walk rather than three lucky coincidences.
- **RULED 2026-09-19T09:01:36-04:00** — MEASURED IN FULL AND FINISHED, v0.14.83. tools/garage-return-test.py now walks seven doors with the real buttons, each on a car the garage did not open on: END > CHANGE CAR, END > MAIN MENU, PAUSE > RESTART, a RELOAD (closing and reopening the app), a TOURNAMENT round lost on the clock and left by QUIT & SAVE, and the PRIZE screen's two buttons followed by a reload. All seven return to the car driven.

ONE DOOR WAS BROKEN, AND ONLY AFTER A RELOAD: the prize screen's DRIVE IT switched to the won car but did not SAVE the choice - the arrows save `body` and this button did not - so reopening the app put the player back in the car they had before the prize. It saves now, and syncs the paint for the new car. With the save removed, "PRIZE > DRIVE IT survives a reload" fails (wanted ROADSTER, got SALOON). KEEP MY CAR is the control and passes either way. `API.showUnlock(k)` opens the prize screen for a harness without a tournament being won.

NOT WALKED: leaving an INTERCEPT shift. It ends through the same `showEnd` card as the END arms, so it takes the same doors; a police car cannot be driven in a fresh save without staging an unlock. Awaiting the owner's verdict.
