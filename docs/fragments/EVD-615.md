+++
id                = "EVD-615"
kind              = "evidence"
status            = "recorded"
step              = "the speed a harness pins four times a second is a range tens of mph wide with the gate inside it; held every frame the spread is zero, and held BELOW the gate nothing is sent"
unit              = "UNT-244"
command           = "P:/Code_Continuum/Projects/In-Dev/Effigy_Arcade/.venv/Scripts/python.exe tools/super-gate-proof.py"
exit_code         = 0
duration_ms       = 65973
output_bytes      = 1107
output_sha        = "e2b7c4c738112ec1a91193087761b3c6"
store_stamp       = "fd0222cc237d35b9"
created           = "2026-09-09T22:05:23-04:00"
created_precision = "second"
supersedes        = []
blocks            = []
caused_by         = []
see_also          = []
+++

### EVD-615 — step `the speed a harness pins four times a second is a range tens of mph wide with the gate inside it; held every frame the spread is zero, and held BELOW the gate nothing is sent` of [[UNT-244]]

- Ran: `P:/Code_Continuum/Projects/In-Dev/Effigy_Arcade/.venv/Scripts/python.exe tools/super-gate-proof.py`
- Result: **exit 0**, in 65973 ms
- Output: 1107 bytes, sha256 `e2b7c4c738112ec1a91193087761b3c6`
- Store stamp at the time: `fd0222cc237d35b9`

**Output** — head only; the digest above covers the whole of it, so this excerpt cannot be selective without saying so.

```
super-gate-proof  .  the check read zero because the car was not going fast enough
  browser: playwright bundled chromium (pinned)
      the rule under test: above 150mph for 4 unbroken seconds, at heat 3 or more
      OLD  asked for 160mph: the car read 144-169mph, under the gate on 1 of 64 samples; the longest unbroken stretch banked 11.12s of the 4s needed; 4 supers
  ok    a speed asked for four times a second is a range, not a speed
  ok    and the bottom of that range eats the headroom over the gate
      NEW  asked for 160mph: the car read 160-160mph, under the gate on 0 of 64 samples; the longest unbroken stretch banked 10.93s of the 4s needed; 4 supers
  ok    holding it keeps the car above the gate on every sample
  ok    so the unbroken seconds the rule asks for are actually banked
  ok    and an interceptor is dispatched
      SLOW asked for 140mph: the car read 140-140mph, under the gate on 64 of 64 samples; the longest unbroken stretch banked 0.00s of the 4s needed; 0 supers
  ok    below the gate, held just as steadily, nothing is sent
  ok    no page errors

the proof holds
```

*Written by `step.py` as a byproduct of running the step. **Nobody attested to this** ([[RLG-081]]).*
