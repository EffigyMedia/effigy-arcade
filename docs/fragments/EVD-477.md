+++
id                = "EVD-477"
kind              = "evidence"
status            = "recorded"
step              = "heat-test's cooling budgets are read from the tunable, and only the pre-existing super-cruiser check still fails"
unit              = "UNT-200"
command           = "P:/Code_Continuum/Projects/In-Dev/Effigy_Arcade/.venv/Scripts/python.exe tools/heat-test.py"
exit_code         = 1
duration_ms       = 152332
output_bytes      = 1082
output_sha        = "60285e4ff6948db30f3c6502c635fc30"
store_stamp       = "9ccb56dee029ceb7"
created           = "2026-09-08T01:43:07-04:00"
created_precision = "second"
supersedes        = []
blocks            = []
caused_by         = []
see_also          = []
+++

### EVD-477 — step `heat-test's cooling budgets are read from the tunable, and only the pre-existing super-cruiser check still fails` of [[UNT-200]]

- Ran: `P:/Code_Continuum/Projects/In-Dev/Effigy_Arcade/.venv/Scripts/python.exe tools/heat-test.py`
- Result: **EXIT 1 — the step FAILED**, in 152332 ms
- Output: 1082 bytes, sha256 `60285e4ff6948db30f3c6502c635fc30`
- Store stamp at the time: `9ccb56dee029ceb7`

**Output** — head only; the digest above covers the whole of it, so this excerpt cannot be selective without saying so.

```
heat-test  .  the wanted level is earned, not counted
  browser: playwright bundled chromium (pinned)
  ok    the pursuit system is running
      heat 2 -> 2 over 11s stopped on an empty road (cooling at 10.4 of 30)
  ok    time alone never raises the wanted level
      heat 4 -> 3 after 21s clear of every cruiser (a star is 30s)
  ok    outrunning them cools the wanted level
      a cruiser held 20,000 behind: the cool clock reached 47.8 of the 30 needed
  ok    a cruiser you have left behind stops holding the wanted level up
  FAIL  and it falls all the way to empty, which it could not before   [1 stars, 109 points]
      heat 5 at 160mph, no trap earned: 0 supers
  ok    no super cruiser without the 170 past a trap
      with the trap earned: 0 supers
  FAIL  and one is dispatched once it has been   [{'heat': 5, 'pts': 500, 'cool': 60.01, 'earned': True, 'chasing': 0, 'supers': 0, 'coolNeeds': 30, 'easy': False}]
      heat held at 2 with the trap earned: 0 supers, heat now 2  (printed, not asserted - see the note above)
  ok    no page errors

2 check(s) failed
```

*Written by `step.py` as a byproduct of running the step. **Nobody attested to this** ([[RLG-081]]).*
