+++
id                = "RLG-243"
kind              = "ruling"
type              = "feature"
status            = "built"
created           = "2026-09-14T08:49:50-04:00"
created_precision = "second"
source            = "owner, 2026-09-14"
supersedes        = []
blocks            = []
caused_by         = []
see_also          = []
+++

### RLG-243 — Police cars flash small emergency lamps at both ends, in step with the light bar
- **RULED 2026-09-14T08:50:36-04:00** — **BUILT 2026-09-14, v0.14.18. NOT SIGNED** ([[RLG-116]]).

Owner: *"add emergency lights within the headlights and tail lights of the police vehicles so a little blue and a little red lamp - matching the side the colors are on the light bar."* The owner was unsure whether the rear lamps belonged beside the brake lights or in the rear glass, and asked for both to be rendered. After the side-by-side capture, the owner placed each car's lamps:

| Car | End | Placement | Owner's words |
|---|---|---|---|
| CRUISER | front | a small lens at the inboard end of each headlight | "I like the front lights" |
| CRUISER | rear | a short bar each side at the foot of the rear window | "prefer the rear lights in the window" |
| SUPER CRUISER | front | a bar the shape of the turn indicator, under it, at the lamp's rake | "a bar like the turn indicator, under the turn indicators" |
| SUPER CRUISER | rear | the innermost chevron of each cluster, taken from the brake lamp | "the first inner chevron instead of the brake one" |

## HOW IT IS BUILT

- **Each small lamp is its own declared lamp**, named after the half of the bar it flashes with: `em.rl` follows `bar.rl`, `em.fl` follows `bar.fl`. `emOf(ids)` adds the matching `em.*` ids, and the three places that light a bar use it: `drawCopLights`, the mirror, and the player's own car. The lamp and its half of the bar cannot fall out of step.
- **They are separate declarations, not more drawing inside the bar's.** A painter bakes the bar BEFORE its lamp clusters, so an unlit lens drawn with the bar would be covered by the cluster housing.
- **The colour is the lens's own side of the bar**, from `BAR_SCHEME`: from behind the car's left is blue, and from the front it is red, the same mirroring the bar has.
- **Police only.** They are keyed on `kind === 'cop'` in the rig painters and on `force` in the car painters. The ambulance keeps its bar and gets no small lamps.
- On the SUPER CRUISER the brake lamp now draws seven blades a side instead of eight. The indicator still owns the outer two.

## A DEFECT FOUND ON THE WAY, AND FIXED

**A patrol cruiser in the mirror never flashed its bar.** `paintRigFront` painted the two lenses straight into the sprite with no declaration, so the mirror asked `FRONT_SP.cop` for `bar.fl` and `bar.fr`, found neither, and lit nothing. `mirror-bar-test` passed, because it checks that nothing is painted ABOVE the roof, not that the bar lights. The lenses are declared now, with the same rectangles and the same unlit colours, so the dark sprite is unchanged.

## THE PROOF, AND WHAT IT CANNOT SAY

- `tools/fleet-sheet.py` now lights `em.*` in its bar cells. Regenerated, only `docs/fleet/police.png` changed; the other five sheets came out byte-identical, so no other vehicle's sprite moved.
- lamp-test (EVD-790), mirror-bar-test (EVD-791), car-ends-test (EVD-792), face-test (EVD-793) and smoke-test (EVD-797) pass.
- brake-test failed once on this tree (EVD-794) and passed on a rerun (EVD-796). It also passed on the last commit, 90a73703 (EVD-795). **EVD-795's title says the test "fails the same way" there. That is wrong: it passed, exit 0.** The title was written before the run. The failure is a flaky stopping-distance ranking, not this change.
- drive-test is 36/37 (EVD-798). The failing check is the nitrous settle at 101.1% of top speed. It has failed the same way in eleven earlier runs, EVD-306 to EVD-751.
- Two option captures were rendered through a temporary `EM_REAR` switch, which is removed. Five step runs, EVD-785 to EVD-789, have exit 127: the step runner could not find a relative interpreter path. They ran nothing.

**No harness asserts that the small lamps light.** The fleet sheet shows them lit, and nothing measures them in play. Whether they read at road distance on a phone, and whether the red chevron reads apart from the red brake blades beside it, is the owner's verdict on the device.
- **RULED 2026-09-14T09:56:45-04:00** — **TWO CORRECTIONS FROM THE OWNER, BUILT 2026-09-14, v0.14.19. NOT SIGNED** ([[RLG-116]]).

**1. THE CRUISER'S LIT HEADLIGHT COVERED ITS EMERGENCY LENS.** Owner: *"the emergency lights in the headlights are obscured when the headlights are bright - can we shrink the lit bright headlight lamp to the truncated version of the headlight lamp."* The lit `head` lamp is redrawn over the sprite at runtime, and at full width it painted over the lens at its inboard end. On a cruiser, `paintRigFront`'s `head` now gives up that segment in BOTH states (`hIn = FT`), so the lamp is still one drawing in two states and the lens has the segment to itself. Other rig cars keep the full lamp: `hIn` is 0 unless `kind === 'cop'`.

**2. THE UNLIT SMALL LAMPS WERE TOO BRIGHT.** Owner: *"the color you have for the emergency lights when they are not lit is the appropriate color for when they are lit minus a bit of bloom they should have. The unlit emergency lamps should have the same darkness as the unlit headlight and indicator lamps."* The small lamps took the BAR's lens pair, whose unlit colour is a saturated blue and red. They now have their own pair, `BAR_SCHEME.police.em`:

| Lens | Lit | Unlit |
|---|---|---|
| blue (`em.a`) | `#2f6bff`, the bar's unlit blue | `#16337a` |
| red (`em.b`) | `#ff2b4a`, the bar's unlit red | `#7a1423` |

Unlit is the lit colour scaled until its strongest channel is `#7a`, the rule `RED_OFF` and `AMBER_OFF` are already dark by ([[RLG-198]]). The bloom is the halo `sprite()` bakes for every lit lamp, so nothing extra draws it. The Interceptor's emergency chevron now uses one colour for its stroke and its core, lit or unlit; its lit core was white. **The light bar itself is unchanged.**

## THE PROOF

- lamp-test failed once (EVD-799): 320 headlight frames "lit at midday". That counter counts frames in which the mirror ASKS for headlights, which `lampsOn()` gates on the hour and the weather, and no drawing changed there. It passed on the next run with 0 at midday (EVD-802). A timing flake, not this change.
- face-test (EVD-800) and smoke-test (EVD-801, EVD-803) pass.
- The regenerated police fleet sheet shows the lens beside the lit headlight on both cruiser liveries and the dark unlit lamps on both cars.

**No harness measures these colours.** Whether the unlit lamps read as lamps and the lit ones as flashing, on a phone, is the owner's verdict on the device.
- **RULED 2026-09-14T10:06:23-04:00** — **THE LIGHT BAR TAKES THE SAME COLOURS, BUILT 2026-09-14, v0.14.20. NOT SIGNED** ([[RLG-116]]).

Owner, on build 0.14.19: *"you need to apply the same color changes to the actual light bar as well."* `BAR_SCHEME` now gives the bar the pair the small lamps got in v0.14.19, and the separate `em` pair is removed, so the bar and its small lamps share one pair again:

| Scheme | Lens | Lit (was) | Unlit (was) |
|---|---|---|---|
| police | `a`, blue | `#2f6bff` (`#8fb6ff`) | `#16337a` (`#2f6bff`) |
| police | `b`, red | `#ff2b4a` (`#ff8fa4`) | `#7a1423` (`#ff2b4a`) |
| medical | `a` and `b`, red | `#ff2b4a` (`#ff8fa4`) | `#7a1423` (`#ff2b4a`) |

The ambulance follows the same rule, because a medical bar is the same lamp in another colour. This is my decision, not the owner's, and the owner can reverse it. The wash is light thrown on the road, not a lens, and is unchanged.

**The owner also said:** *"The cruiser also gets intercept mode, unless we wanna call it cruise, but that might be unnecessary."* No change was needed: `dutyLegal` offers INTERCEPT to every body with `force`, and the CRUISER has it. The name stays Intercept.

## A LEFTOVER OF RLG-177, FOUND AND FIXED

The mirror still painted a full-width rectangle ABOVE a ROADBLOCK cruiser's roof, in `#3b6bff` or `#ff2b4a`, under the comment that the bar "is not in the sprite". This is the stray bar [[RLG-177]] removed from the traffic cruisers; the roadblock path was missed. It now calls `lampsLit` on `FRONT_SP.cop` with `emOf([bar.fl or bar.fr])`, which is possible because that sprite declares its bar lamps since v0.14.18. `mirror-bar-test` does not cover it: it reads `mirrorCopBox`, which only the traffic cruiser path sets.

## THE PROOF

- mirror-bar-test (EVD-804), lamp-test (EVD-805) and smoke-test (EVD-806) pass.
- Of the six fleet sheets, only `police.png` and `utility.png` changed. The utility sheet holds the ambulance.

**No harness shows the roadblock cruiser in the mirror.** Its bar is proved only by reading the code. The colours are the owner's verdict on the device.
