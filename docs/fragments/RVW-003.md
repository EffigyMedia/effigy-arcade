+++
id                = "RVW-003"
kind              = "review"
status            = "recorded"
unit              = "UNT-419"
created           = "2026-09-22T11:23:11-04:00"
created_precision = "second"
source            = "review of UNT-419, scoped to `review.py --packet`"
supersedes        = []
blocks            = []
caused_by         = []
see_also          = ["UNT-419"]
files             = ["road.js", "tools/livery-grid-test.py"]
findings          = 5
packet            = "0b1ec44fe84b81d9"
reviewed_lines    = 265
verdict           = "sound-with-findings"
+++

### RVW-003 — review of [[UNT-419]] — sound-with-findings

- **Scope**: 2 file(s), 265 code line(s). The `files` field is the diff at the moment this was filed, read from git rather than typed by the reviewer.
- **Packet**: `0b1ec44fe84b81d9`. The digest of the packet the reviewer was handed was recomputed when this was filed and matched, so the diff recorded above is the diff that was read.
- **Findings**: 5.

Fresh-eyes review of UNT-419 by a separate agent. Verdict: sound with findings. Nothing that worked is broken: the paintGlow extraction is exact, the cache key covers everything carSprites and liveryPass read, formula bodies get no livery, rigs use the player's own rig path, drag rivals take none, and the mirror's racer branch is right.

1. MEDIUM - RACER_LIVERY_SP was never evicted; at about 300 KB per entry a phone could pass WebKit's canvas memory limit over many races. FIXED: the cache is emptied at each new grid.
2. MEDIUM - sprites were built lazily inside a frame, for plain rivals too, so the first frames after GO could hitch. FIXED: a rival with no stripes and no two-tone uses the prebuilt cache, and the rest are built in buildField, before the count-in.
3. LOW - the rival's underglow was drawn before drawSprite set the car's alpha, so it showed at full strength while the car faded or smoked. FIXED: the glow takes the rival's own fade.
4. LOW - API.racerLivery took null as 0. FIXED: numbers only.
5. HARNESS - check 4 read an API that does not exist and let any one paint go missing; check 5 cannot fire on the default grid; the falsify arm's 'only check 1' is asserted by falsify-expect in the evidence, not by the harness; the draw path was untested. FIXED: check 4 excludes exactly the player's paint (API.paint); a new check counts rivals DRAWN in their livery sprite and with a glow. Check 5 is kept as a guard and says so.
