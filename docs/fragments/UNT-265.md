+++
id                = "UNT-265"
kind              = "unit"
status            = "closed"
cost              = 95870
closed            = "2026-09-11T11:53:27-04:00"
created           = "2026-09-11T00:05:54-04:00"
created_precision = "second"
source            = "the thread's stated next action"
supersedes        = []
blocks            = []
caused_by         = ["RLG-203"]
see_also          = []
delegate          = false
effort            = "medium"
model             = "claude-sonnet-5"
ran_on            = "claude-opus-5"
routing           = "feature"
+++

### UNT-265 - The field's own lawlessness decides how many police are out

- Routing class: `feature` -> **claude-sonnet-5**, effort **medium**. Bias 1. Implementation to a spec that already answers the design questions. ([[RLG-092]])
- Opened for: [[RLG-203]]
- Opened by `unit.py`. **The identity exists before the first edit** ([[RLG-097]]), so the snapshot has somewhere to go.
- **CLOSED 2026-09-11T11:53:27-04:00** - The field's own lawlessness decides how many police are out, which was the last question RLG-203 had left open. It counts the rivals still running AND still over SPEED_LIMIT - the same threshold a patrol and a trap measure every other driver by - and drives the wanted level the whole pursuit system already reads. Only heat's SOURCE moved; the traps, roadblocks, interceptor gate and cruiser skill all read the same number meaning the same thing.

THE DENOMINATOR IS THE WHOLE FIELD, NOT THE SURVIVORS, and that is what makes the mode breathe: a full grid flat out is five stars and a complement of four, the last rival is a duel with the two wingmen. Measured against what is LEFT, one car and eleven would both read five stars and the road would never thin out. The complement sits on top of the floor - WINGMEN + dispatchCap(), capped at the four the box can station.

TWO INSTRUMENT FAILURES, both of which produced confident wrong numbers. A hold that pins the TARGET is not a hold: `holdField` first pinned `want` and let the physics chase it, so a field meant to be flat out measured four of eleven over the limit and the check swung 4/8/11 between runs. It assigns `spd` outright now, the way the player's `holdSpd` always has. And a field staged in the player's LINE is not a field - eleven cars in one lane queue and dip under the limit - so `stageField` puts them ahead and across the lanes.

AND A WAIT IN WALL SECONDS MEASURED NOTHING: the level is charged per ENGINE second and simTime runs at a fraction of wall time here, so 24s of waiting was ~6s of engine and the first run read one star where it expected five. The harness waits on the engine clock now, with a wall-clock cap.

ALSO IN THIS COMMIT, and named because it is not this feature: `harness.until` waited out its full timeout on a CRASHED page, turning a browser crash into a ten-minute hang. A context going away mid-navigation is still not a failure; a dead page is.

THE FOUR shift-stop-test FAILURES SEEN BEFORE THIS CLOSE WERE NOT A DEFECT. They were measured while this machine was in the state RLG-211 records - the driving cabinet taking 207 to 339 seconds to boot on the accelerated canvas path. It cleared, interstate boots in 1.0s again, and shift-stop-test is green.

NOT SIGNED OFF (RLG-116). What is left in RLG-203: RLG-171's traffic-damage guard read again from the side where the PLAYER is the cruiser.
