+++
id                = "EVD-924"
kind              = "evidence"
status            = "recorded"
step              = "a lost tournament round offers three attempts, quitting saves the ladder under its class, and the check still fails with the tournament branch taken out"
unit              = "UNT-336"
command           = ".venv\\Scripts\\python tools/tour-save-test.py"
exit_code         = 0
duration_ms       = 50893
output_bytes      = 1573
output_sha        = "1d5691e1509ca43e921c012f471e310b"
store_stamp       = "b52f8eceb9dbb084"
created           = "2026-09-16T03:37:08-04:00"
created_precision = "second"
supersedes        = []
blocks            = []
caused_by         = []
see_also          = []
+++

### EVD-924 — step `a lost tournament round offers three attempts, quitting saves the ladder under its class, and the check still fails with the tournament branch taken out` of [[UNT-336]]

- Ran: `.venv\Scripts\python tools/tour-save-test.py`
- Result: **exit 0**, in 50893 ms
- Output: 1573 bytes, sha256 `1d5691e1509ca43e921c012f471e310b`
- Store stamp at the time: `b52f8eceb9dbb084`

**Output** — head only; the digest above covers the whole of it, so this excerpt cannot be selective without saying so.

```
tour-save  .  a lost round retries, and a quit tournament waits per class
      serving P:\Code_Continuum\Projects\In-Dev\Effigy_Arcade
  browser: installed chrome - no bundled chromium, so the engine version is whatever this machine has
      LOST A ROUND    card offers [again,quit], retries 3 of 3, round 0
  ok    losing a tournament round offers a retry and a way out
  ok    and it is the tournament card, not the ordinary end card
  ok    the attempt is not spent by losing, only by pressing retry
      RETRY           retries 3 -> 2, round 0 -> 0, points 0 -> 0
  ok    pressing retry spends one attempt
  ok    and re-runs the SAME round
  ok    and the round that was lost scored nobody
      QUIT            saved rows ['sports']
  ok    quitting the tournament saves it
  ok    and the attempts left are saved with it
      AFTER A RELOAD  round 0, points 0, retries 2
  ok    and a reload comes back to the ladder where it was left
      A SUPERCAR      class super, round 0, points 0, retries 3
  ok    a supercar puts you in the super league
  ok    and gets its OWN tournament, not the sports one
  ok    and the sports ladder is still where it was left
      SINGLE RACE     card offers [again,garage,menu]
  ok    losing a single race gives the ordinary end card
      OUT OF RETRIES  retries 0, card offers [quit]
  ok    the attempts run out
  ok    and the last one takes the retry button away
  ok    and running out clears the saved tournament for that class
  ok    no page errors

a lost round retries, and a quit tournament waits under its class
```

*Written by `step.py` as a byproduct of running the step. **Nobody attested to this** ([[RLG-081]]).*
