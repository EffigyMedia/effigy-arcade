+++
id                = "EVD-659"
kind              = "evidence"
status            = "recorded"
step              = "the garage card is one end, centred, on a common floor, with a button that flips it and a front that returns on reopen"
unit              = "UNT-271"
command           = "P:\\Code_Continuum\\Projects\\In-Dev\\Effigy_Arcade\\.venv\\Scripts\\python.exe tools/garage-card-test.py"
exit_code         = 0
duration_ms       = 17256
output_bytes      = 951
output_sha        = "01127d144589e6913ea9f6ba5bc35c28"
store_stamp       = "84c0f3efd97b1f01"
created           = "2026-09-12T09:30:38-04:00"
created_precision = "second"
supersedes        = []
blocks            = []
caused_by         = []
see_also          = []
+++

### EVD-659 — step `the garage card is one end, centred, on a common floor, with a button that flips it and a front that returns on reopen` of [[UNT-271]]

- Ran: `P:\Code_Continuum\Projects\In-Dev\Effigy_Arcade\.venv\Scripts\python.exe tools/garage-card-test.py`
- Result: **exit 0**, in 17256 ms
- Output: 951 bytes, sha256 `01127d144589e6913ea9f6ba5bc35c28`
- Store stamp at the time: `84c0f3efd97b1f01`

**Output** — head only; the digest above covers the whole of it, so this excerpt cannot be selective without saying so.

```
garage-card  .  one end, centred, on the floor, with a button to flip it
  browser: installed chrome - no bundled chromium, so the engine version is whatever this machine has
  ok    TUNER paints ONE car, at the width the fit asks for
  ok    TUNER is drawn wider than the old half allowed
  ok    TUNER is centred on the card
  ok    MUSCLE paints ONE car, at the width the fit asks for
  ok    MUSCLE is drawn wider than the old half allowed
  ok    MUSCLE is centred on the card
  ok    ROADSTER paints ONE car, at the width the fit asks for
  ok    ROADSTER is drawn wider than the old half allowed
  ok    ROADSTER is centred on the card
  ok    every car stands on the same floor line
  ok    there is a flip button on the card
  ok    pressing it turns the car round
  ok    changing the paint leaves the rear showing
  ok    reopening the garage shows the FRONT again

the card is one end, centred, on the floor, and the button turns it round
```

*Written by `step.py` as a byproduct of running the step. **Nobody attested to this** ([[RLG-081]]).*
