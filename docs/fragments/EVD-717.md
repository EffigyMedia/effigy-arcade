+++
id                = "EVD-717"
kind              = "evidence"
status            = "recorded"
step              = "with the floor deleted from the stylesheet, both tyre checks fail"
unit              = "UNT-293"
command           = "P:/Code_Continuum/Projects/In-Dev/Effigy_Arcade/.venv/Scripts/python.exe tools/garage-card-test.py --falsify floor"
exit_code         = 1
duration_ms       = 42365
output_bytes      = 1358
output_sha        = "5087507c6e20b267a477a48cb6ac921d"
store_stamp       = "130ec37355c66f64"
created           = "2026-09-12T23:46:02-04:00"
created_precision = "second"
supersedes        = []
blocks            = []
caused_by         = []
see_also          = []
+++

### EVD-717 — step `with the floor deleted from the stylesheet, both tyre checks fail` of [[UNT-293]]

- Ran: `P:/Code_Continuum/Projects/In-Dev/Effigy_Arcade/.venv/Scripts/python.exe tools/garage-card-test.py --falsify floor`
- Result: **EXIT 1 — the step FAILED**, in 42365 ms
- Output: 1358 bytes, sha256 `5087507c6e20b267a477a48cb6ac921d`
- Store stamp at the time: `130ec37355c66f64`

**Output** — head only; the digest above covers the whole of it, so this excerpt cannot be selective without saying so.

```
garage-card  .  one end, centred, on the floor, with a button to flip it
  browser: installed chrome - no bundled chromium, so the engine version is whatever this machine has
  ok    SALOON paints ONE car, at the width the fit asks for
  ok    SALOON is drawn wider than the old half allowed
  ok    SALOON is centred on the card
  ok    COUPE paints ONE car, at the width the fit asks for
  ok    COUPE is drawn wider than the old half allowed
  ok    COUPE is centred on the card
  ok    HATCH paints ONE car, at the width the fit asks for
  ok    HATCH is drawn wider than the old half allowed
  ok    HATCH is centred on the card
  ok    every car stands on the same floor line
  ok    there is a flip button on the card
  ok    pressing it turns the car round
  ok    changing the paint leaves the rear showing
  ok    reopening the garage shows the FRONT again
  ok    a locked car has NO flip button
  ok    and its card still paints the silhouette
  ok    and an owned car gets it back
      floor 20.2  card 11.6  spread 13.4
  FAIL  the card has a lit floor under the car   [floor 20.2 against card 11.6 - that is the card's own gradient, not a floor]
  FAIL  the tyres read against the floor   [only 13.4 between the darkest and the lit floor - they are one smear]

FAILED: the card has a lit floor under the car; the tyres read against the floor
```

*Written by `step.py` as a byproduct of running the step. **Nobody attested to this** ([[RLG-081]]).*
