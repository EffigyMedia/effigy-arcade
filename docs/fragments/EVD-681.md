+++
id                = "EVD-681"
kind              = "evidence"
status            = "recorded"
step              = "the production class measures in the owner's three orderings: 0-60 8.9/9.6/11.3s, top 110/120/124mph, and the hatch answers the wheel fastest and stops shortest"
unit              = "UNT-280"
command           = "P:/Code_Continuum/Projects/In-Dev/Effigy_Arcade/.venv/Scripts/python.exe tools/brake-test.py --group production"
exit_code         = 0
duration_ms       = 50950
output_bytes      = 2391
output_sha        = "db1d714c3c3a7d60bc3efad54c661c43"
store_stamp       = "9ecb20d407519d6b"
created           = "2026-09-12T13:26:12-04:00"
created_precision = "second"
supersedes        = []
blocks            = []
caused_by         = []
see_also          = []
+++

### EVD-681 — step `the production class measures in the owner's three orderings: 0-60 8.9/9.6/11.3s, top 110/120/124mph, and the hatch answers the wheel fastest and stops shortest` of [[UNT-280]]

- Ran: `P:/Code_Continuum/Projects/In-Dev/Effigy_Arcade/.venv/Scripts/python.exe tools/brake-test.py --group production`
- Result: **exit 0**, in 50950 ms
- Output: 2391 bytes, sha256 `db1d714c3c3a7d60bc3efad54c661c43`
- Store stamp at the time: `9ecb20d407519d6b`

**Output** — head only; the digest above covers the whole of it, so this excerpt cannot be selective without saying so.

```
brake-test  .  what the fleet does when it stops and when it turns
  group: production  .  3 bodies, all of them measured at 100mph
  browser: installed chrome - no bundled chromium, so the engine version is whatever this machine has
  the cornering push on one constant bend, at 100 mph, wheel untouched  (speed HELD, road EMPTY)
      COUPE         pushed 0.2166 of a lane over 2.5s   (grip 0.730)   99 -> 100 mph
      HATCH         pushed 0.2029 of a lane over 2.5s   (grip 0.780)   100 -> 100 mph
      SALOON        pushed 0.2390 of a lane over 2.5s   (grip 0.660)   100 -> 100 mph
  ok    every body in the group was pushed
  the spread across the fleet is 0.0361 of a lane
  push x grip should be one number for the whole fleet: mean 0.1580, spread 0.3%, furthest out SALOON at 0.1577
  ok    the cornering push is the same number for every car once grip is taken out
  ok    and a car the table says grips CLEARLY better is pushed wide less

  how sharply each car answers the wheel, on a straight road at 100 mph
      COUPE         3.28 lanes a second across 1.70 of the road, 38 frames   (grip 0.730, the engine says 3.28)
      HATCH         3.39 lanes a second across 1.70 of the road, 36 frames   (grip 0.780, the engine says 3.39)
      SALOON        3.11 lanes a second across 1.70 of the road, 36 frames   (grip 0.660, the engine says 3.11)
  ok    every body in the group answered the wheel
  ok    a car the table says grips clearly better answers the wheel faster
  the fleet spans 3.11 to 3.39 lanes a second, a spread of 1.09 to 1
  ok    the spread inside one class is reported, not asserted
  ok    and not so large that the fleet is two different games
  measured against the engine's own declared ceiling: worst is HATCH, 0.0% out
  ok    and the car really does move at the rate the engine says it can

  braking from 100 to 40 mph, dry, on a flat straight road

      COUPE           2034 ms      3664 units   brakeOf says 0.752
      HATCH           1768 ms      3421 units   brakeOf says 0.811
      SALOON          1802 ms      4035 units   brakeOf says 0.680
  ok    every body in the group could be measured

  the shortest stop is the HATCH at 3421 units; the longest is the SALOON at 4035
  the table ranks 3 of 3 in the order they actually stop
  ok    a car the table says brakes better actually stops shorter

  ok    no page errors

all checks passed
```

*Written by `step.py` as a byproduct of running the step. **Nobody attested to this** ([[RLG-081]]).*
