+++
id                = "EVD-921"
kind              = "evidence"
status            = "recorded"
step              = "every door out of an Interstate run keeps the car, and the falsifier proves the file cannot score a walk that did not happen"
unit              = "UNT-333"
command           = ".venv\\Scripts\\python tools/garage-return-test.py"
exit_code         = 0
duration_ms       = 31626
output_bytes      = 615
output_sha        = "b2264cac025f7773e64e54327232e711"
store_stamp       = "62b713bdda739ac4"
created           = "2026-09-16T02:18:15-04:00"
created_precision = "second"
supersedes        = []
blocks            = []
caused_by         = []
see_also          = []
+++

### EVD-921 — step `every door out of an Interstate run keeps the car, and the falsifier proves the file cannot score a walk that did not happen` of [[UNT-333]]

- Ran: `.venv\Scripts\python tools/garage-return-test.py`
- Result: **exit 0**, in 31626 ms
- Output: 615 bytes, sha256 `b2264cac025f7773e64e54327232e711`
- Store stamp at the time: `62b713bdda739ac4`

**Output** — head only; the digest above covers the whole of it, so this excerpt cannot be selective without saying so.

```
garage-return  .  every door back to the garage keeps the car
      serving P:\Code_Continuum\Projects\In-Dev\Effigy_Arcade
  browser: installed chrome - no bundled chromium, so the engine version is whatever this machine has
      END > CHANGE CAR  drove COUPE, garage shows COUPE
  ok    END > CHANGE CAR returns to the car driven
      END > MAIN MENU   drove SALOON, garage shows SALOON
  ok    END > MAIN MENU returns to the car driven
      PAUSE > RESTART   drove HATCH, garage shows HATCH
  ok    PAUSE > RESTART returns to the car driven
  ok    no page errors

every door back to the garage keeps the car
```

*Written by `step.py` as a byproduct of running the step. **Nobody attested to this** ([[RLG-081]]).*
