+++
id                = "EVD-945"
kind              = "evidence"
status            = "recorded"
step              = "a car you have hit is still solid, and leaning on it is one event"
unit              = "UNT-342"
command           = "P:\\Code_Continuum\\Projects\\In-Dev\\Effigy_Arcade\\.venv\\Scripts\\python.exe tools/contact-test.py"
exit_code         = 1
duration_ms       = 14403
output_bytes      = 874
output_sha        = "e516ceab318c656260921cd8b916ce8b"
store_stamp       = "fe59c5a593363a99"
created           = "2026-09-16T10:13:08-04:00"
created_precision = "second"
supersedes        = []
blocks            = []
caused_by         = []
see_also          = []
+++

### EVD-945 — step `a car you have hit is still solid, and leaning on it is one event` of [[UNT-342]]

- Ran: `P:\Code_Continuum\Projects\In-Dev\Effigy_Arcade\.venv\Scripts\python.exe tools/contact-test.py`
- Result: **EXIT 1 — the step FAILED**, in 14403 ms
- Output: 874 bytes, sha256 `e516ceab318c656260921cd8b916ce8b`
- Store stamp at the time: `fe59c5a593363a99`

**Output** — head only; the digest above covers the whole of it, so this excerpt cannot be selective without saying so.

```
contact-test  .  a car you have hit is still solid

  browser: installed chrome - no bundled chromium, so the engine version is whatever this machine has
  221 frames sampled, 6 of them with the two bodies overlapping
  FAIL  the player actually reached the car and stayed on it   6 frames in contact

  IS IT SOLID
      closest the two centres came, as a fraction of the overlap: 0.35
  ok    the player never gets deep inside the other car   closest 0.35 of the overlap width, and 0.00 is the same point
  FAIL  the player never comes out the far side of a car it drove into   ended -10611 units from it

  IS IT ONE EVENT
      1 crash response(s) over 4.0s, 6 frames of contact
  ok    driving into a car costs a hit at all   1
  ok    leaning on a car is one event, not one a frame   1 responses against 6 frames of contact
  ok    no page errors

  2 check(s) FAILED
```

*Written by `step.py` as a byproduct of running the step. **Nobody attested to this** ([[RLG-081]]).*
